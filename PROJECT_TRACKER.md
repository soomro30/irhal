# Irhal Portal V4 Feature Tracker

Last updated: 2026-06-13

This tracker reflects the current local implementation against the Irhal Enterprise City Guide V4 requirements. Status values are `Not Started`, `In Progress`, `Completed`, or `Blocked`.

## Current Build Snapshot

- Local app: Next.js 16 App Router, TypeScript, Tailwind CSS, and owned shadcn-style UI primitives.
- City homepage hero left panel was recolored from the dark `bg-ink` surface to a brighter `#81BEFF` light-blue surface across `/`, `/ar/city/...`, and `/en/city/...` (single file edit in `src/app/(frontend)/city/[city]/page.tsx` since the AR/EN routes reuse `CityPageContent`). Text was inverted from white-on-dark to ink-on-light for contrast: country eyebrow and heading at full `text-ink`, lede paragraph at `text-ink` with `font-medium`, fact row labels at `text-ink/80` with full-ink values, divider raised to `border-ink/25`. After visual review the panel chrome was rebalanced so the "London" heading stays the visual anchor: the next-prayer chip was softened from a solid dark pill into a quiet `bg-white/90` chip with a thin `border-ink/15`, ink label text, and a `#00EB5B` accent circle behind the mosque icon (a secondary signal, not a competing focal point); the "More about the city today" link was promoted to a solid `bg-ink` button so the reading hierarchy is heading → lede → fact row with the prayer/weather chips as quiet accents. The arbitrary `#81BEFF` literal is consistent with the existing in-file precedent of arbitrary Tailwind utilities (`bg-[#00EB5B]`, `bg-[#16325c]`) — it stays in the Tailwind arbitrary-value form rather than introducing a global theme token.
- (Superseded) Itinerary planner showcase featured-route card was first recolored from `bg-ink` to an editorial yellow `#FFCE26`. This was rejected on review ("yellow with black is not good") and fully replaced by the clean white redesign below.
- Itinerary planner showcase (`src/components/itinerary-planner-showcase.tsx`) was redesigned into a clean modern travel-planner module. The decorative fake "route map" center column — a CSS grid background with absolutely-positioned fake pins and an overlapping photo card — was removed entirely because it carried no real information and read as cheap filler. The section moved from a 3-column to a 2-column layout on a white chapter band with subtle `border-y border-ink/10`, separating it from the grey search/explore area above while preserving calm editorial continuity. The featured route is now a horizontal white card (`rounded-2xl border-ink/10`): a crisp `object-cover object-center` image cell beside the content (image requests a `50vw` candidate so cover has full-resolution pixels and no longer reads blurry/upscaled), a "FEATURED ROUTE" badge overlaid on the image, then title, a compact duration · stops meta line, a 4-line summary for denser enterprise copy, audience/theme chips, and — replacing the fake map — a real route built from the itinerary's actual resolved stops rendered as compact wrapping numbered pills (green numbered dot + stop name). The featured image column was reduced to `0.72fr` so the content feels more operational and less image-heavy, and long route-stop pills now cap/truncate cleanly. Stops that resolve to the Arabic "pending translation" placeholder (`…قيد الترجمة`) are filtered out so the Arabic route never leaks CMS-missing copy. The horizontal layout plus pill-based route roughly halved the card height so it balances against the right-column "More route ideas" stack. Alternate routes on the right are compact horizontal cards (image + ROUTE PREVIEW eyebrow + title + `duration • stops`) with the shared hover-lift treatment. Fully EN/AR + RTL aware; verified with `npm run typecheck` and `eslint` clean.
- Itinerary image selection now prefers approved primary CMS media from route stops before falling back to gallery images. This keeps large featured itinerary cards from selecting soft/editorial gallery crops when a sharper stop image exists; London's "Classic first day" route now skips the Westminster Abbey telephone-box gallery crop and resolves to another approved stop image from the same itinerary. Verified with `npm run typecheck` and `npm run lint -- src/components/itinerary-planner-showcase.tsx src/lib/itineraries.ts`.
- London City Information `Residents` article now has approved Payload/R2 media instead of the generic city-information placeholder. The article source payload uses the Old Spitalfields Market shoppers image (`london-old-spitalfields-market-shopping-brendan-bell-640x432.webp`) with a targeted `update-logs` entry, and local revalidation refreshed English/Arabic London city and Residents article routes. Browser QA confirmed the Arabic `السكان` card renders the shoppers image.
- London Arabic guide-item QA is closed for the current Payload corpus. Public QA first found `/ar/city/london/place/westminster-abbey` missing Arabic detail copy because its canonical Payload `guide_items` row still had null Arabic fields; Westminster Abbey was repaired directly in Payload, then the remaining 41 London guide items with missing effective Arabic core fields were repaired with Gulf-editorial Arabic title, summary, overview, area, category, and address/location fields where applicable. The 11 long-source records that still failed strict overview parity were expanded, and the London city shell `translations.ar` fields (`name`, `lede`, `description`, `countryName`, `currency`, `languages`, `bestTimeToVisit`) were completed. The repair is documented in Payload `update_logs`; London city/guide/search/itinerary cache tags and affected paths were revalidated locally. Final verification passes `npm run audit:london-arabic-quality` with 733 total items and 0 issue items, `npm run audit:london-arabic-articles` with 0 article issues, `npm run audit:london-arabic-shell-itineraries` with 0 city issues and 0 itinerary issues, plus `npm run typecheck`, `npm run lint -- scripts/repair-london-arabic-guide-item-gaps.ts scripts/repair-london-arabic-final-qa-gaps.ts`, and rendered HTML checks for `/ar/city/london`, `/ar/city/london/place/westminster-abbey`, and `/ar/city/london/place/london-eye`.
- Shared `FeatureCard` (in `src/components/feature-rail.tsx`) was rebuilt to a GetYourGuide-pattern card so every guide rail on the city homepage, inside item-detail recommendation rails, neighborhood rails, and practical info rails get a single consistent compact silhouette: bordered white card (`rounded-xl border-ink/10`) with edge-to-edge top image, eyebrow rendered as an overlay chip in the top-left of the image (`bg-ink/85 text-white`), a non-functional floating heart icon in the top-right (RTL-mirrored), body block with title + 3-line description + `DiscoverPill` anchored at the bottom. Hover darkens border to `ink/25` and lifts a soft ambient shadow.
- `FeatureRail` section header action ("Explore all") was promoted from an underlined text link to an outlined `irhal-red` pill button that fills red on hover, matching the GetYourGuide outlined-pill pattern.
- Card typography across both `FeatureCard` and the section-grid `GuideItemCard` (`src/components/guide-item-card.tsx`) was compressed for density: titles dropped from `text-lg font-black` (18px/900) to `text-[15px] font-bold` (15px/700) with `tracking-[0.005em]` micro letter-spacing so individual characters get breathing room without scaling the title up; descriptions dropped from `text-sm leading-6 text-ink/70` to `text-[13px] leading-[1.45] text-ink/85` (booking/airbnb-style ~`#3D3D3D` near-black with normal weight, the retention-optimal body color for travel UIs and softer on the eye than pure black); card padding tightened from `p-4` to `px-3.5 py-3`; title→description gap from `mt-2` → `mt-1.5`; bottom CTA gap from `pt-5`/`pt-4` → `pt-3.5`/`pt-3`. The section grid `GuideItemCard` was also unified with `FeatureCard`'s shape — edge-to-edge `aspect-[4/3]` image instead of the previous inset `mx-4 mt-4 h-[174px]` frame, `rounded-xl` shell with the same hover elevation — so city homepage, section pages, and inside-page rails share one rhythm.
- Neighborhood carousel data-quality fix in `src/lib/city-presentation.ts`: `getNeighbourhoodHighlights` was silently surfacing every neighborhood (24 areas) regardless of mapped content, and `neighbourhoodRepresentativeImage` was reusing the city hero image (Mohatta Palace for Karachi) whenever a neighborhood had no media-backed item — so several Karachi areas (Port Grand, Manora, Seaview / Clifton Beach) rendered the same wrong landmark photo. The helper now returns `null` when a neighborhood has no matched guide items OR no matched item with approved Payload/R2 media, and the highlights iterator skips those neighborhoods entirely (replacing the prior `.map(...)` with a typed `for...of` push pattern so the optional `image` field on `NeighbourhoodHighlight` stays sound). The "X areas" count chip in the carousel header auto-updates from the filtered length. Verified with `npx tsc --noEmit` clean.
- English city homepages now place the dark city information panel on the left and the wide hero image carousel on the right, while Arabic keeps the mirrored RTL desktop arrangement. The city hero carousel now auto-advances every 6 seconds when multiple city images are available, including London, while preserving manual controls and reduced-motion handling.
- Missing public guide-item media now uses city-neutral SVG placeholders from `public/images/guide-placeholders` instead of Karachi-branded fallback art. London hotel/section cards without Payload media therefore display generic "Hotels", "Places", "Food", etc. artwork while preserving the rule that real item photos must come from approved Payload/R2 media or vetted repo fallbacks.
- London's city homepage hero no longer uses the soft `london-city-hero.webp` source (`1800×589`, ~118KB) or the people-led temporary banner. London now has a four-image Payload/R2 hero carousel: the user-requested Irhal/Pexels London Eye panorama is the primary `heroImage`, followed by approved high-resolution St Paul's skyline, St Paul's/Big Bus, and Tower Bridge carousel slides. Browser QA on `/ar/city/london` verified all four slides load from the new media records at `1600px` natural width in the current `774px` hero slot with `1920px` candidates available for high-density displays.
- Payload media upload derivatives were raised for future city-quality imagery: thumbnails are now `480×330`, cards `960×648`, heroes `2400×1350`, and original resize bounds `3600×2400`, so new approved uploads retain enough source pixels for retina city cards, hero frames, galleries, and detail pages.
- City-facing image components now request retina-safe optimized source widths across compact and large surfaces instead of barely-display-size assets. The pass covers itinerary showcase thumbnails, itinerary hub cards, itinerary stop thumbnails, guide item cards/rails, feature rails, guide-section/article cards, neighborhood cards, city image cards, gallery thumbnails, section sidebars, item-detail sidebars, public search results, and typeahead thumbnails so high-density screens do not receive soft 48–224px content photos.
- Itinerary detail heroes now render their main photo as a full-view panoramic image layer instead of a hard-cropped `object-cover` background. A softened cover backdrop fills the wide hero frame while the foreground itinerary image uses `object-contain`, preserving the full composition in EN/AR itinerary pages.
- City homepage itinerary sections now use a dedicated TripAdvisor-inspired planning showcase instead of the shared image-card rail. The module stays Payload-driven, highlights one featured route with duration/stops/audience/day-theme chips, renders a map-like route panel with resolved itinerary stop names, and exposes compact alternate route previews plus the all-itineraries CTA. This gives the first itinerary block a distinct route-planning identity while preserving EN/AR and RTL support.
- City hero prayer timing chip is now more prominent with a tight-cropped local Portal Mosque icon asset, green highlighted surface, stronger shadow/ring treatment, and clearer next-prayer/time emphasis in both Arabic and English city headers.
- City homepage ordering now places the City Information / History and Today article rail immediately after Top Places to Visit, so London and other city pages surface today/history/residents context before the food, family, hotel, shopping, visitor, and transport rails. Organized Tours now renders after Masjids and Muslim Travel, keeping tours below the food/halal-aware restaurant and masjid rails.
- City homepages now suppress the visible breadcrumb/context strip so the first screen starts directly with the destination experience, while preserving breadcrumb JSON-LD for SEO and leaving visible breadcrumbs intact on section pages, item detail pages, articles, itineraries, neighborhoods, listings, and other inner routes.
- Arabic is now the default public entry language: `/` renders the Arabic homepage, `/ar` remains available for Arabic locale routes, English public pages are served under `/en`, old unprefixed English public routes such as `/city/karachi` redirect to `/ar/city/karachi`, the header language toggle handles `/`, `/ar`, `/en`, and nested EN/AR routes cleanly, and SEO alternates now use Arabic as `x-default`.
- Public city routes have been hardened against Supabase/Postgres egress spikes: the public root layout no longer reads request headers, `/en` now has its own English root layout instead of relying on proxy rewrites, `/en/city/...` routes are explicit real routes, and all public city/detail routes export empty `generateStaticParams()` so Next treats them as on-demand SSG (`●`) instead of per-request dynamic SSR (`ƒ`). Production public route and Payload data caches now default to 24 hours with publish-triggered revalidation, city data refuses production TTLs below 6 hours unless the floor env is changed, search/site settings caches no longer force a one-hour route cache, and Payload collection hooks now invalidate city/scope-specific tags instead of broad all-city cache tags for every child-content edit. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, production `next start`, SSG route-table output, `/en/city/karachi` cache headers (`s-maxage=86400`, `x-nextjs-prerender: 1`), repeat detail-page cache `HIT`, correct initial EN/AR `<html lang>`/`dir`, and legacy `/city/karachi` redirecting to `/ar/city/karachi`.
- City media selection now prefers Payload-generated `hero` and `card` image derivatives before falling back to original uploads, and city navigation/card data carries a dedicated `cardImageUrl`. Portrait city cards now request a larger optimized image at an allowed higher quality so landscape card derivatives are not upscaled inside tall cards. This prevents wide CMS hero originals from being reused blindly inside portrait city cards and bumps the city data cache keys so production picks up the corrected media payload.
- Public search/typeahead now uses a pooled SQL-backed Payload search path instead of building a full in-memory city/document index through `getCityBySlug()` on user input. The API queries only published city, guide item, neighborhood, guide section, and itinerary fields, maps results directly to public URLs, ranks exact city matches first, caches query results for five minutes, and falls back to Payload indexed hydration only when SQL returns no results. Typeahead UI now shows a loading state instead of a premature "No matching results" message. A Payload migration adds `pg_trgm` GIN indexes for city, guide item, neighborhood, guide section, and itinerary search fields; the same non-destructive indexes were applied to the connected database directly after the Payload CLI refused to run migrations without a data-loss prompt.
- Homepage search results now render through a client-side body portal with viewport-aware positioning instead of being absolutely positioned inside the clipped hero section. This keeps typeahead results above following page sections, preserves keyboard/click behavior, updates position on resize/scroll, and constrains long result sets to the available viewport height.
- Homepage hero imagery now uses the shared city hero carousel instead of rendering only the first featured city image. The first viewport rotates through available city primary hero images with a non-Karachi opening slide when another city is available, uses original panoramic city uploads before generated 16:9 hero derivatives, keeps manual previous/next controls, respects reduced-motion preferences, and renders as a shorter full-width `center center` cover banner with no stretching. Verified with `npm run typecheck`, `npm run lint`, and in-app browser QA on `/`.
- City-page hero media now uses a responsive panoramic frame for city banner photos, so wide originals such as London's `london-city-hero.webp` render close to their source composition instead of being cropped inside a tall fixed image box. The desktop frame was tuned to a taller panoramic ratio (`2.35:1`/`2.45:1` by breakpoint) so the media has more presence while staying banner-like. The carousel still uses `cover center center`, original city uploads remain preferred before generated derivatives, and card imagery remains unchanged. Verified with `npm run typecheck`, `npm run lint`, and in-app browser QA on `/en/city/london`.
- London was imported into Payload from `London_United_Kingdom_City_Guide_Irhal_Enterprise_PLUS_2026 (1).docx` using a new guarded extractor/seed pipeline. The import creates the United Kingdom country record, London city record, 15 Karachi-model guide sections, 544 public guide items, 8 itineraries, 33 borough neighborhood discovery layers, 5 district clusters, London hero/gallery media, and repeatable npm seed scripts. Wikimedia Commons enrichment linked draft licensed media for 233 London guide items so far (111 places, 40 shopping, 34 masjids, 22 hotels, 12 family items, 10 tours, and 4 restaurants); source/license metadata is stored for editorial review. Arabic city and section copy is seeded, while full item-level Gulf editorial Arabic remains blocked until a translation/model provider key is configured.
- London legacy Irhal archive recovery is now a repeatable targeted import via `npm run import:london-legacy`. The update sets the user-requested owned Irhal London hero image, scans the old WordPress/Payload archive for London records, merges legacy editorial copy into existing canonical Payload guide items, creates missing archive-backed guide items, uploads owned legacy images through Payload/R2, and adds first-class festival guide items. Current published London counts are 135 places, 60 hotels, 228 restaurants, 166 masjids, 59 shopping entries, 27 tours, 16 family entries, and 18 festivals. Windsor Castle, Eid in the Square, Open House Festival, Totally Thames Festival, Winter Wonderland Hyde Park, London Film Festival, Notting Hill Carnival, the Proms, Chelsea Flower Show, Wimbledon, and other legacy festival/event entries are now public Payload records with source attribution and operational verification notes. Bulk import scripts now set `IRHAL_SKIP_PAYLOAD_HOOK_REVALIDATE=true` and rely on the protected `/api/revalidate` endpoint afterward, avoiding invalid `next/cache` hook calls outside a route context while leaving normal CMS hook revalidation intact.
- London city-information, visitor-information, and transportation sections now have a targeted Payload import via `npm run import:london-info-transport`. It promotes old Irhal London History, London Today, Annual Temperature/weather, Public Holidays, Currency/Exchange Rates, Fast Facts, Visa Information, When to Go, Buses, Tube, Taxis, Airport transfers, Car Hire, and Driving Tips from article-style archive records into first-class section guide items with source attribution and current GOV.UK/TfL/Visit London verification references where operational details can change. The city category rail now counts guide items for these sections before falling back to old article counts, so the same section-card behavior applies to London, Karachi, and future cities when guide items exist. The city hero carousel now explicitly renders cover images at `center center`.
- London Arabic item translation has started section-by-section in Payload. The first completed slice is `city-information`: `london-today` and `london-history` now have Gulf editorial Arabic title, summary, overview, area, and category fields stored on the canonical guide-item records, and both touched records are marked `review` for editorial workflow. Follow-up audit shows 0 Arabic gaps across the two City Information guide items, and the Arabic public routes render localized metadata/content.
- London Today and London History are now canonical City Information articles, not place-detail pages. Guide-item cards route to `/city/london/section/city-information/city-today` and `/city/london/section/city-information/city-back-then`; the duplicate `/city/london/place/london-today` and `/city/london/place/london-history` URLs redirect with HTTP 308 in both English and Arabic. Their Arabic article translations are stored in `guide_sections.translations.ar.articles`, so the kept City Information article URLs render the Arabic copy instead of the CMS-missing placeholder.
- City/Visitor Information canonical placement is now enforced for Karachi and London. City Today, City Back Then/History, and Residents live under `section/city-information`; practical travel notes such as visa/ETA, when to go, exchange rates, fast facts, public holidays, and annual temperature/rainfall live under `section/visitor-information`. Karachi already followed this model. London's legacy item duplicates for these information pages now route cards to the canonical section articles and redirect the duplicate `/city/london/place/...` URLs with HTTP 308 in both English and Arabic.
- London Arabic translation pass completed for the public information sections. `city-information` now has Arabic article translations for City Today, City Back Then/History, and Residents. `visitor-information` now has Gulf editorial Arabic article translations for visa/ETA, when to go, exchange rates, fast facts, public holidays, and annual temperature/rainfall, including Arabic table labels and values for fast facts, holidays, and climate. The Arabic `when-to-go` article also carries the same climate table shown on the English page. The shared guide table renderer now maps Arabic headers such as `الشهر`, `متوسط الحرارة العظمى`, `المعلومة`, and `التاريخ` back to their stored row keys so Arabic tables render populated cells instead of blank rows. The six duplicate Visitor Information guide-item rows were mirrored with Arabic title/summary/overview/area/category and moved to `review`. Payload audits show 0 Arabic gaps across the 3 City Information articles, 6 Visitor Information articles, and 8 associated information guide items; public Arabic article routes render without the CMS-missing placeholder.
- London Arabic translation pass completed for `transportation-and-getting-around`. All 7 canonical section articles (Tube/Elizabeth line, buses, National Rail/Overground, taxis/ride-hailing, Heathrow, Gatwick, and driving tips) now have Gulf editorial Arabic title, summary, and block translations in Payload. The 6 legacy/mirror transport guide-item records (car hire, driving tips, airports, buses, taxis, Tube) now have Arabic title, summary, overview, area, category, and address/location notes and are marked `review`. Exact duplicate old place routes for buses, taxis, Tube, and driving tips redirect to the canonical section-article URLs in English and Arabic. Arabic related-item rails now suppress untranslated CMS-placeholder guide cards, so Arabic section/article/detail pages do not leak `قيد الترجمة` or CMS-missing copy while London item translation continues section-by-section. Verified with Payload audits, `npm run typecheck`, post-restart HTML checks, and in-app browser QA on `/ar/city/london/section/transportation-and-getting-around`.
- London Arabic translation pass completed for `health-and-safety`. All 8 article-only safety pages now have Gulf editorial Arabic title, summary, translated body blocks, and translated map/source-note link text stored in Payload, covering emergency response, terrorism/security, phone theft, food/water, street food, women/families, weather, and scams/unsafe transport. The section is marked `review`; official UK/NHS/MI5/Met/TfL sources were used to keep emergency-number, threat-level, phone-theft, and minicab guidance conservative. Verified with a zero-gap Payload audit, `npm run typecheck`, fresh post-restart HTML checks for the section and representative articles, and in-app browser QA confirming RTL Arabic content with no CMS-missing placeholders.
- London Arabic translation pass completed for `neighborhood-operating-guide`. The borough live-discovery article now has Gulf editorial Arabic title, summary, explanatory body copy, and a fully translated 33-row table covering the 32 boroughs plus the City of London, including Arabic table headers, borough names, live halal-restaurant map copy, masjid/prayer-room map copy, and translated link text stored in Payload. The shared table renderer now maps the Arabic borough/live-map headers back to their stored row keys so Arabic cells remain populated. The section is marked `review`; the 33-authority structure was checked against official London City Hall/London Councils sources. Verified with a zero-gap Payload audit, `npm run typecheck`, post-restart HTML checks, and in-app browser QA confirming RTL Arabic table content with no CMS-missing placeholders.
- London Arabic translation pass completed for `children-in-tow`. All 16 family guide items now have Gulf editorial Arabic title, summary, overview, area, category, and address/location notes stored in Payload and are marked `review`, including the longer legacy `what-to-do-with-kids-in-tow` item. Verified with a zero-gap Payload item audit, `npm run typecheck`, and post-restart public HTML checks for the section plus representative detail pages (`natural-history-museum`, `what-to-do-with-kids-in-tow`, and `london-zoo`) with no CMS-missing placeholders. Browser automation timed out on this warmed route, but server-rendered route checks returned 200 and confirmed the Arabic content.
- London Arabic translation pass completed for `organized-tours`. All 27 tour guide items now have Gulf editorial Arabic title, summary, overview, area, category, and location-note fields stored in Payload and are marked `review`, covering museum routes, Islamic/halal-aware food routes, family/film tours, stadium tours, river cruises, and legacy tour-company records. Verified with a zero-gap Payload audit, `npm run typecheck`, and post-restart public HTML checks for `/ar/city/london/section/organized-tours` plus representative tour detail pages (`british-museum-highlights-tour`, `hmc-halal-food-crawl-east-london`, and `tower-of-london-yeoman-warder-tour`) with no CMS-missing placeholders.
- London Arabic translation pass completed for `festivals-and-annual-events`. All 13 monthly/index section articles now have Gulf editorial Arabic title, summary, and paragraph block translations in Payload, and all 18 festival/event guide items now have Arabic title, summary, overview, area, category, and address/location notes stored on the canonical records and marked `review`. Verified with a zero-gap Payload item/article audit, `npm run typecheck`, and post-restart public HTML checks for the Arabic section page, January and festival-index article routes, and representative detail pages (`eid-in-the-square`, `open-house-festival`, and `winter-wonderland-hyde-park`) with no CMS-missing placeholders.
- London Arabic translation pass completed for `shopping`. The section intro article and all 59 shopping guide items now have Arabic title, summary, overview, area, category, and address/location notes stored in Payload and are marked `review`, spanning luxury retail, Muslim shopping corridors, markets, malls, outlets, gifts, food halls, and family-friendly shopping areas. Verified with a zero-gap Payload audit, `npm run typecheck`, and post-restart public HTML checks for the Arabic section page plus representative detail pages (`harrods`, `southall-broadway`, `borough-market`, `westfield-stratford-city`, and `bicester-village`) with no CMS-missing placeholders.
- London Arabic translation pass completed for `hotels`. The section intro article and all 60 hotel guide items now have Arabic title, summary, overview, area, category, and address/location notes stored in Payload and are marked `review`, covering luxury heritage hotels, central international hotels, budget/compact hotels, and serviced-apartment stays. Verified with a zero-gap Payload audit, `npm run typecheck`, and post-restart public HTML checks for the Arabic section page plus representative detail pages (`claridge-s`, `hyatt-place-london-city-east`, `citadines-trafalgar-square-london`, `shangri-la-the-shard`, and `the-z-hotel-trafalgar`) with no CMS-missing placeholders.
- London Arabic translation pass completed for `places-to-visit`. The section intro article and all 135 place guide items now have Arabic title, summary, overview, area, category, and address/location notes stored in Payload and are marked `review`, covering royal landmarks, museums, parks, viewpoints, markets, heritage districts, sports venues, and riverside routes. Verified with a zero-gap Payload audit, `npm run typecheck`, and post-restart public HTML checks for the Arabic section page plus representative detail pages (`buckingham-palace`, `british-museum`, `london-eye`, `hyde-park`, `borough-market`, and `whitechapel-road`) with no CMS-missing placeholders.
- London Arabic QA has been tightened after the item-detail translation gap was found. Repeatable `npm run audit:london-arabic-quality` and `npm run audit:london-arabic-articles` commands now block missing Arabic fields, shallow Arabic overviews for long English source records, known generic template phrases, and visible Latin leakage across London guide-item and article Arabic fields. The London Payload records were repaired in place: all 724 guide items now pass the strict item audit with 0 failures and 0 visible Latin item fields; London section/article Arabic translations pass a 0-issue article audit after localized replacements for transport, government, map, festival, currency, and safety terminology; footer-generated Arabic city links now render `دليل لندن`, `تسوق لندن`, and `فنادق لندن` instead of English labels. Verified with both audit commands, `npm run typecheck`, targeted revalidation, and server-rendered smoke checks on representative Arabic place, restaurant, visa, transport, festival, masjid, hotel, and shopping routes.
- London Arabic frontpage and itinerary QA now covers the surfaces missed by the earlier item/article audits. The city hero uses Payload `translations.ar.lede`, `countryName`, `currency`, `languages`, and `bestTimeToVisit`; the practical-info rail no longer leaks raw `GBP`, `English, Arabic`, or `Europe/London`; and all 8 London itineraries now have Arabic title, audience, summary, intro, planning, meals, and day-level route translations stored in Payload. A repeatable `npm run audit:london-arabic-shell-itineraries` command checks these city-shell and itinerary fields for missing Arabic, placeholder copy, and Latin leakage. Verified with the new audit, `npm run typecheck`, a cleared Next data cache, fresh dev server on `http://localhost:3001`, and rendered checks for `/ar/city/london`, `/ar/city/london/itineraries`, and `/ar/city/london/itineraries/classic-first-day` confirming no `مسار قيد الترجمة`, no English hero paragraph, and no raw `GBP`/`United Kingdom`/`English, Arabic` in the tested visible copy.
- London Arabic city hero lede was expanded directly in Payload as a targeted city-shell update, preserving the existing city record and replacing only `translations.ar.lede` / `translations.ar.description` with a fuller Gulf editorial Arabic equivalent to the approved English London lede. An `update-logs` entry records the source snapshot and verification date; the Arabic hero text is now length-balanced against English. Verified with Payload read-back and `npm run audit:london-arabic-shell-itineraries` reporting 0 city issues and 0 itinerary issues.
- Karachi city hero lede was expanded from the short one-sentence placeholder into a richer neighborhood-led city introduction in both the repo fallback and Payload city shell. The Arabic version is stored as Gulf editorial MSA in `translations.ar.lede` / `translations.ar.description`, with Arabic-script place-name transliterations and no English fallback for the Arabic hero copy. An `update-logs` entry records the targeted city-shell update.
- Arabic homepage city cards now use Payload city translations through the shared `CityNavItem` data instead of one-off hardcoded labels. London now renders as `لندن` with `المملكة المتحدة` in featured destination cards, city guide menus, and footer city links, while Karachi remains localized as `كراتشي`/`باكستان`. Verified on live dev routes `/ar` and `/` with no `London` or `UNITED KINGDOM` in the featured-destination card text.
- London itinerary routes now store non-listing guide-item stops in Payload day rows through a new `stopSlugs` field, fixing the imported London routes that previously rendered as `0 stops` because their raw seed arrays and relationship rows were empty. The public city loader uses relationship stops first and falls back to newline-separated `kind:slug` stop slugs, the London seed script now writes those stop slugs for future protected imports, the connected Payload database was backfilled for all 8 London itineraries, and browser QA confirms `/en/city/london` shows real `5/6` stop counts while `/en/city/london/itineraries/classic-first-day` renders resolved stops such as Westminster Abbey and Buckingham Palace.
- London itineraries are now enriched Payload-only, not JSON-backed: the `itineraries` collection stores intro copy, planning cards (stay, transport, breakfast/lunch/dinner), and day-level description/start/transport/meals/pacing fields; the city loader hydrates those CMS fields directly; `npm run import:london-itineraries` updated all 8 existing London itinerary records and their day rows in Payload without re-running a baseline seed or changing workflow/source/media relationships. The guarded London seed path also writes the same fields for explicit future resets. Verified with Payload SQL read-back, `npm run generate:types`, `npm run typecheck`, `npm run lint`, `npm run build`, and production HTML checks on `/en/city/london/itineraries/classic-first-day` confirming route-planning copy such as "Best base: Westminster", "Hotel breakfast before departure", and "Full day but walkable" renders alongside real stop counts.
- London hero media is locked to a single approved city banner: the legacy London import now clears `heroGallery` instead of preserving older gallery images, so the public city hero cannot rotate into low-quality/unapproved banner photos. A repeatable `npm run audit:london-irhal-places` command compares old Irhal London `/places-to-visit/` WordPress records against Payload by legacy WP id, source URL, mapped slug, and title, including the child-travel page that is intentionally represented as a family guide item.
- Public portal UI now uses a custom Irhal travel identity on top of shadcn-style `Button`, `Badge`, and `Card` primitives rather than a generic SaaS/admin look.
- The public UI now has a clearer conversion-focused radius/color/elevation system applied through shared design tokens and primitives: global canvas/body tokens move to `#FAFAFA` / `#111111`, Tailwind radius tokens map regular cards to 8px and overlays/dropdowns to 16px, shared buttons and badges render as pill actions/tags, default cards use a softer 8px elevated surface, dropdown/search overlays use ambient shadows, the sticky header uses the 0px shell layer with a subtle `0 4px 20px rgba(17,17,17,0.05)` shadow, and the homepage search CTA now uses the Irhal coastal action color instead of an unrelated bright blue. Verified with `npm run typecheck`, `npm run lint`, and in-app browser QA on `http://localhost:3001/en` plus `/en/city/karachi`: desktop tokens apply, typeahead returns Mohatta results in a 16px overlay, and a 390px mobile viewport has no horizontal overflow.
- Repeated city and guide-card imagery now follows the editorial engagement radius rule: hero/media mastheads stay sharp, while city cards, guide rails, section cards, neighborhood panels, practical-info panels, and item/article grid thumbnails use 8px image corners with restrained hover elevation. Dense city-section cards keep the subtle 8px white card shell, but their images are inset and rounded so the click target feels deliberate without becoming app-like. Verified with `npm run typecheck`, `npm run lint`, and browser QA on `/en` plus `/en/city/karachi/section/places-to-visit`: repeated images resolve to 8px radius, card shells remain 8px, badges stay inside image bounds, and there is no horizontal overflow.
- City hero desktop panels now use a simple straight image boundary instead of the previous elliptical clipped edge, removing the muddy curved/shadowed transition on pages such as `/en/city/london` while preserving the existing RTL mirror behavior.
- The public header has been redesigned into a modern Lonely-Planet-style clean white sticky bar with backdrop blur, replacing the solid deep-red navigation. Brand identity is preserved through a compact red logo chip (white Irhal PNG on an `irhal-red` rounded badge) and red accents/active states. The desktop nav uses dark-ink links with animated red underline hovers, a Payload-backed City Guides Radix/shadcn DropdownMenu with map-pin city rows, a News link, a real EN/العربية language toggle pill, and an `Explore {city}` red CTA. A new shadcn `DropdownMenu`-based mobile hamburger menu adds previously-missing mobile navigation (Home, News, city guides, Explore CTA, language toggle). All header logic lives in the new client `SiteHeader` component and remains fully RTL-mirrored. The old `city-guide-menu.tsx` was folded into `SiteHeader` and removed. The language toggle now strips existing locale prefixes and rebuilds the opposite canonical locale URL, so `/ar/city/karachi` switches to `/en/city/karachi` instead of falling through the unprefixed Arabic redirect path. A root client locale synchronizer immediately updates `<html lang>`/`dir` during client-side locale navigation so stale RTL/LTR styles cannot persist while the next page streams.
- Arabic header city navigation now localizes Payload city rows for the Arabic UI (Karachi → `كراتشي`, Pakistan → `باكستان`) and renders the desktop and mobile city guide menus with RTL direction/alignment. Frontend route transitions now show immediate feedback through a global top progress indicator plus a public `loading.tsx` skeleton so dynamic server-rendered routes do not appear idle while waiting for the server response. Vercel runtime regions are now pinned in `vercel.json` to `bom1` for nearby South Asia/Middle East execution after Vercel rejected `dxb1` as currently unavailable.
- The item/content detail page (`GuideItemDetail`, used for place/hotel/restaurant/masjid/shopping/tour/family/festival pages) was rebuilt to an enterprise CMS/travel-site standard. It now has: a refined hero (eyebrow, title, lede, scannable Area/Type fact chips, rounded hero image, red "Open map" CTA); a two-column body with the verbatim "Overview" content + a "Good to know" card on the left and a sticky sidebar on the right containing a curated "Quick facts" panel (Area, Type, Price as `$`-symbols, Address + map button — no raw column dump), a "More to explore" cross-category list (~6 items with clear type labels), and a "Plan your trip" CTA; and cross-discovery below the fold — a "You might also like" region with same-category-first rails ("More places/restaurants/hotels in {city}") plus a "Related guides" grid of articles from the same section. Added proper per-item `schema.org` JSON-LD (`guideItemJsonLd` in `seo.ts`, mapping kind → TouristAttraction/Hotel/Restaurant/PlaceOfWorship/ShoppingCenter/Festival/TouristTrip) alongside the breadcrumb JSON-LD. Shared kind-label maps (`guideKindOrder`, `kindPlural`, `kindSingular`) were centralized in `guide-items.ts` and reused by both the detail and section pages. Fully EN/AR + RTL aware.
- City guide section pages (`section/[section]`) were redesigned to match the city-guide template and now use a two-column `main + sidebar` layout instead of an isolated single column. Both section types share it: item sections render the guide-item grid and article sections render article cards, each led by the red "CITY GUIDE SECTION" eyebrow + bold heading. A new reusable `SectionSidebar` component provides an "Explore the guide" section nav (current section highlighted), a "More to explore" cross-discovery card, and a dark "Plan your trip" CTA (View itineraries / Open city map); it is sticky on desktop, stacks on mobile, and is EN/AR + RTL aware.
- Article-only guide sections such as Transportation now use the same image-first editorial card rhythm as the item sections instead of plain bordered text cards. Article cards show a 4:3 section visual, uppercase section eyebrow, bold two-line title, two-line summary, and the shared `Discover` pill, while preserving the article detail links.
- The city homepage now surfaces article-only guide content as first-class rails instead of leaving those sections only in the icon menu and final guide-section grid. Visitor Information, Getting Around/Transportation, and History and Today/City Information each render as image-first `FeatureRail` cards linking to their article detail pages, with dedicated section artwork so these cards no longer fall back to misleading generic "Karachi Places" or "Karachi Tours" placeholders.
- Karachi and London City Information articles now preserve the richer owned Irhal legacy Today/History pages directly in Payload. The repeatable `npm run import:city-history-today` command imports Karachi Today, Karachi History, London Today, and London History from the legacy Irhal URLs, uploads their owned page images to Payload/R2 as approved media, stores article-level `imageUrl`/`imageAlt` on `sourceImport.articles`, and updates the existing `city-today` / `city-back-then` article entries without changing public slugs. Because London City Information is item-driven, the same importer also refreshes the existing `london-today` and `london-history` guide items with the richer body copy, source rows, and article images. Public article cards now prefer article images, and article detail pages render the article image above the guide body.
- London City Information now surfaces supplemental article-only entries such as Residents below the Today/History guide-item grid, while filtering out duplicate article cards for London Today and London History. Article and guide-card image rendering now treats empty Payload image strings as missing media, falling back to approved section/place artwork instead of passing `src=""` to Next/Image. Single-paragraph article pages such as Residents reuse their body copy in the content panel instead of rendering a large empty card.
- Item-driven sections no longer publish their own section intro as duplicate article pages. Legacy/imported `sourceImport.articles` entries such as London `/section/hotels/hotels` and Karachi `/section/hotels/6-hotels` are recognized as section-intro pseudo-articles when the section already has real guide items, so they are filtered out of public article helpers, recommendations, and direct article routes while the real section pages remain available.
- Section pages were hardened for production: (1) confusing bare price-tier labels (e.g. "EXPENSIVE") are converted to clear price symbols (`$`–`$$$$`) in guide card meta, and the sidebar "More to explore" items now show an unambiguous type label (Hotel, Restaurant, Shopping, Place, …) instead of the raw category; (2) item sections are now paginated at 24 per page via static SSG-safe `/p/{page}` URLs, with a "Showing X–Y of Z" header and an accessible Previous / numbered / Next control (windowed with ellipses), threaded through the Arabic route too; (3) the redundant "Guides and visitor notes / Read deeper visitor notes" block was removed from item sections — after the item list the page goes straight to "Keep exploring {city}". Article-only sections still render their article cards (now without the duplicate heading). Verified in server-rendered HTML: "Showing 1–24 of 110 places" (p1) / "Showing 25–48 of 110 places" (p2), hotel cards rendering `$$$`, sidebar type labels, and the removed notes block.
- Engagement/cross-linking modules were added to every section page to drive more pageviews (Expedia/Lonely-Planet style). The sidebar "More to explore" shows 5–6 items pulled from OTHER categories of the same city (thumbnail + title + category, each linking to its detail page). Below the main content + sidebar, a "Keep exploring {city}" region renders up to three horizontal cross-section item rails (e.g. on Places: More restaurants / More hotels / More shopping), each with an "Explore all" link to that section. Related content is derived once via `getGuideItems`, excludes the current section's kind, is localized, and reuses the existing `GuideItemRail`/card components.
- The breadcrumb bar (in `page-shell.tsx`) was demoted from a second white bar into a quiet soft-gray context strip (`bg-paper-deep`) so the white sticky header reads as the single primary chrome and the breadcrumb is clearly secondary. The current-page item is now a white chip with a subtle border/shadow so it pops against the gray, link items use lighter weight with a `hover:bg-white` state, and the bar is slightly shorter. This produces a clean three-layer depth model (white header → gray breadcrumb → white content) and stays RTL-aware.
- The city page top section was later changed to a compact desktop two-column hero on a light `#F2F2F2` band: the left side is a readable deep `bg-ink` city information panel with Irhal green used only as an inline weather badge accent (Open-Meteo current temperature), plus country eyebrow, large `5xl→6xl` title, unclamped lede, and Currency/Language/Best-time facts. The right side is a wider, prominent city banner image (`lg:h-[420px]`, `xl:h-[460px]`) with a compact prayer-times table placed below the image rather than over it. The prayer table is backed by the existing AlAdhan service, links to the full timetable, highlights the next prayer cell, and includes London timezone support (`Europe/London`) alongside Karachi. On mobile it stacks cleanly. The earlier "Plan your trip" / "Open map" buttons under the image were removed at the user's request. The "Explore {city}" navigation is merged into the same gray hero band below the image row and rendered by `CityExploreRail` as a compact horizontal chip rail with prev/next arrows. Because icon components can't cross the server→client boundary, categories were converted to serializable `iconKey` strings and the rail maps those to clean single lucide icons inside small colored circular icon blocks, with labels and readable count pills on white rounded tags.
- The city explore navigation rail was refined from white chip buttons with small colored icon dots into full-color category tiles. Each item now uses its category tone as the whole tile surface, carries an enlarged lucide icon inside a condensed square tile, and keeps the count/live label as a compact high-contrast badge, making the Arabic and English city guide rails more dynamic while preserving the existing client-side `iconKey` mapping and RTL-aware controls.
- (Superseded) The city page top section was first rebuilt into a Lonely-Planet-style **full-bleed** two-column block (modeled on LP's destination header) that merges the old separate hero + category-rail sections into one. The grid spans the full viewport width edge-to-edge (no centered `max-w-7xl` gutters) so it reads like LP's destination header; internal column padding (`px-6 sm:px-8 lg:px-12`) keeps text breathing. On desktop (`lg:grid-cols-2`) it is two equal-height columns; on mobile it stacks. **Left** is a dark `bg-ink` identity panel whose background covers the whole column, ordered **text → image → buttons**: uppercase tracked country eyebrow, a large `5xl→6xl` title, a 4-line lede, a Currency / Language / Best-time fact row above a hairline divider, then the rounded `CityHeroCarousel` (`frameClassName="absolute inset-0"`, `50vw`, `flex-1` to fill column height), and finally `red` "Plan your trip" + `quiet` "Open map" buttons under the image. **Right** is a white panel with the red "CITY GUIDE" eyebrow, the "Explore {city}" heading, an intro line, and the category icons as a responsive grid (`grid-cols-3 sm:grid-cols-4`) of `aspect-square` `rounded-2xl` tiles (subtle `ring-black/[0.06]`, top-corner RTL-aware count chip, icon incl. the custom city-info/transport/halal/masjid glyphs, capitalized label). Per the user's color decision, the tile palette was refined from the earlier bright/neon set into a calmer, more premium coordinated jewel-tone spectrum (warm red `#b83a3a` → burnt orange `#bc5a24` → ochre `#a8782b` → gold `#94792e` → rose `#a03457` → magenta `#ac3568` → dusty pink `#b0527a` → indigo `#2c4778` → steel blue `#2a6594` → teal-blue `#2a82a2` → teal `#2a807c` → green `#2a7449` → olive-green `#557f32` → olive `#6e8a33`) that keeps per-category color-coding, fixes white-text contrast on the previously-neon yellow/lime tiles, and ties to the navy left panel. EN/AR + RTL aware; verified via `npm run typecheck`, lint, and DOM/visual checks of `/en/city/karachi` (full-width two-column desktop; clean stacked order on mobile).
- The homepage and city-guide rails were unified on a single Lonely-Planet-style card system via a new client `FeatureRail`/`FeatureCard` pair (`feature-rail.tsx`): full-bleed sharp-cornered `4:3` image-top cards with an uppercase eyebrow, bold heading, optional 3-line description, and a "Discover" pill, laid out in a snap-scroll rail that shows exactly four full cards at `xl` (3 at `lg`, 2 at `sm`, 1 + peek on mobile) with prev/next controls and an "Explore all" action. Global `max-w-7xl` was widened to `90rem` and `.tracking-tight` relaxed to `0.01em` for LP-style heading air. The shared `GuideItemRail` renders through `FeatureRail`, so every item rail picked up the new look automatically. The city homepage's bespoke blocks were then migrated onto the same system **with descriptions** as requested: the **Karachi itineraries** rail (eyebrow = `1 DAY · 8 STOPS`, 3-line summary, "Open itinerary"), the **Signature sights** rail (eyebrow = category, 3-line overview), and restyled grids for **City travel guide sections** (`GuideSectionGrid`, sharp 4:3 art + 3-line summary) and the **Weather, Money, Safety and Basics** (`PracticalCityInfoCarousel`, icon hero + 3-line body) carousel. **Neighbourhoods** (`NeighborhoodCarousel`), which have no photos, get a matching LP card with a navy→coastal gradient hero carrying the area index (`01`, `02`, …) and "Area guide" eyebrow, plus the 3-line description. All are EN/AR + RTL aware; verified via `npm run typecheck` and on-page screenshots of `/en/city/karachi`.
- Public guide cards now show a consistent two-line summary under the title across feature rails and grid cards. A new Payload Site Settings field, `guideCardSortMode`, controls public card ordering (`Media first`, `More description first`, `Recent update first`, or `Name A-Z`); the default is `Media first`, so city, section, and related-item rails prioritize records with real attached media before placeholder-only records for a more polished public page.
- London and Karachi hotel guide items now use a fixed four-tier public budget taxonomy in Payload: `Budget Hotels` (`$`), `Moderate Hotels` (`$$`), `Expensive Hotels` (`$$$`), and `Luxury Hotels` (`$$$$`). All 85 London/Karachi hotel rows were normalized, and public guide cards now render category and budget as separate tags instead of converting category words like “luxury” into ambiguous dollar text.
- Public city and guide-detail imagery now requests retina-safe optimized variants at quality `90` across feature rails, guide cards, itinerary cards/details, section cards/articles, city hero images, galleries, city image cards, and neighborhood heroes. The Payload guide-item media gate now rejects primary media below 900×540 so old 300px/650px legacy images are not stretched into blurry cards, and future Payload media derivatives use higher WebP quality defaults for cleaner replacements.
- Payload Media stores uploads in Cloudflare R2 via the S3-compatible adapter. Media records include attribution, source, license, usage status, WebP image sizes, and editor-managed image relationships for cities, guide items, neighborhoods, and listings. The default upload pipeline caps originals at 1800×1200, stores originals/hero derivatives at WebP quality 82, card derivatives at 78, thumbnails at 70, and uses effort 6 encoding.
- Replaced London Natural History Museum guide-item media with the user-supplied VisitBritain photograph `VB74HK.jpg` after metadata inspection confirmed the image shows the Natural History Museum entrance sign in London. The new approved R2 media record stores VisitBritain attribution/photographer metadata, embedded camera/software/date notes, and rights-review notes, and is linked to both the `place/natural-history-museum` and `family/natural-history-museum` Payload guide items.
- Replaced London Tower Bridge and Tower of London media with six user-supplied VisitBritain/Historic Royal Palaces assets. Tower Bridge now uses the VisitBritain/George Johnson dusk Thames view as primary and stores two additional Thames/RIB images in the Payload gallery; Tower of London now uses the Historic Royal Palaces/Lea Hair Yeoman Warders image as primary and stores the Crown Jewels entrance and Yeoman Warder tour images in the Payload gallery. All uploaded media records include embedded description/camera/copyright notes where present, approved `partner-provided` status, and preserved photographer/attribution metadata.
- Removed the erroneous London `family/science-museum` Payload guide item and its Ontario Children’s Museum media record after QA found the public page showing the wrong country/image. The canonical London Science Museum record now remains only as `place/science-museum`, and the old family URL redirects to the canonical place page in English and Arabic. Revalidated London guide-item/search caches and verified locally that the old URL resolves to `/en/city/london/place/science-museum`, uses `place-science-museum` media, and renders the Science Museum summary copy.
- Site-wide SEO and public analytics settings are now managed through the Payload `site-settings` Global under the Settings admin group. It stores default SEO title/description, default Open Graph image, canonical site origin, Google Search Console/Bing/Yandex/Pinterest verification values, GA4/GTM public IDs, analytics/consent toggles, and Organization JSON-LD fields. The frontend root layout, canonical metadata base, sitemap, and robots host/sitemap URL read this Global through a cached Payload source; the layout emits Next metadata verification tags, renders validated analytics scripts only when enabled, and publishes Organization structured data. Private secrets remain in Vercel/env, not Payload.
- Payload is the strict public content boundary when configured: city nav, city pages, guide items, sections, listings, neighborhoods, SEO, translations, and media resolve from Payload CMS/R2. Guide items are Payload-only: public item lists and detail pages no longer synthesize, enrich, or translate guide items from local Karachi JSON, legacy update JSON, or extracted guide tables. Local Karachi JSON remains available for unconfigured non-item bootstrap context and as a defensive itinerary route fallback when Payload itinerary day stops are empty, so published itinerary pages do not collapse to `0 stops`. Missing item/city media renders the clean per-kind SVG placeholder; CMS records stay photo-empty until an editor attaches proper R2 media in Payload.
- Payload-backed city loading now hydrates `fullGuide.sections` from the first-class `guide-sections` collection instead of relying only on the legacy embedded `cities.structuredSections` JSON field. This restores section routes such as `/en/city/karachi/section/places-to-visit` and `/ar/city/karachi/section/places-to-visit` from Payload CMS data while preserving the old structured payload as a compatibility fallback.
- City imports now follow a formal all-cities Payload protection policy: the first baseline import for any city may create/replace that city only while it has no Payload child content; after that first successful import, the city is protected and baseline seed scripts must refuse to overwrite it unless an explicit destructive reset command or explicit field-level update is requested. This applies equally to Karachi, London, Dubai, Cairo, Makkah, Istanbul, and every future destination. The reusable guard lives in `scripts/lib/payload-seed-safety.ts`, Karachi now uses it, and the operating rule is documented in both `AGENTS.md` and `docs/cms-import-policy.md` for all future city import scripts.
- Payload admin media stability fix: Media thumbnails now return the stored R2 derivative URL directly for admin previews instead of falling back to `/payload-api/media/file/...`, public API reads no longer apply draft `_status` filters to non-versioned collections, and CMS-backed city hero carousels no longer merge local fallback images once Payload media exists. Verified locally that `/payload-api/cities?...depth=2` returns 200, the Karachi hero media thumbnail URL is R2-backed, and `/ar/city/karachi` renders a single Payload hero image with no 4-image carousel counter.
- Payload guide-item editing is now wired through to public item detail pages: matching CMS records override imported fallback title, summary, rich-text overview body, area/category/budget/map fields, image alt text, primary image, and gallery media, while imported Karachi JSON remains only a gap-filling fallback. Arabic item copy can be edited through `translations.ar.summary` plus `translations.ar.overview` or `translations.ar.body`. The default local development city cache is now 5 seconds instead of 30 minutes so admin edits surface quickly while production keeps the longer cache.
- Karachi guide item overview recovery is now stored in Payload, not read from local runtime data. The targeted `scripts/backfill-karachi-guide-item-body.ts` update filled empty Payload `guide-items.body` fields and missing addresses from the extracted Karachi original-content dataset while preserving any item that already had CMS body copy. The first run scanned 411 guide items, updated 58 empty Payload bodies, skipped 37 existing CMS bodies, and left unmatched records untouched. Verified `/en/city/karachi/place/mohatta-palace-museum` now renders the full Mohatta overview paragraph and Hatim Alvi Road address from Payload.
- Karachi places-to-visit and family/children guide item copy was upgraded directly in Payload with a targeted review-safe script at `scripts/backfill-karachi-place-family-editorial-copy.ts`. The update scanned 123 Karachi `place`/`family` records, improved weak English summaries, added missing English Lexical overview bodies, added Gulf editorial Arabic summaries/overviews, added missing source rows, preserved existing bespoke CMS bodies, and moved touched records to `review` workflow status for editorial approval. Post-update audit shows zero weak English summaries, zero missing English bodies, zero weak Arabic summaries, zero missing Arabic overviews, and no internal editorial-note text across the requested set.
- Karachi Port Trust Building editorial copy was upgraded in Payload and in the repo fallback data: the placeholder summary "Port authority landmark" was replaced with a traveler-facing heritage summary, a three-paragraph overview was added, and a Heritage of Sindh source was attached for traceability.
- Karachi Port Trust Building now has Gulf-ready Arabic editorial fields in Payload and matching repo fallback translation: Arabic title, summary, area/category, address, and a three-paragraph overview replace the English fallback on `/ar/city/karachi/place/karachi-port-trust-building`.
- CMS publishing now has first-class cache invalidation: Payload after-change/delete hooks on public content collections invalidate the `irhal-city` and `irhal-city-nav` data tags plus affected English/Arabic/root city paths. The extra process-memory city cache was removed so Next's data cache revalidation is authoritative, and production no longer silently falls back to local JSON when Payload/Supabase is unavailable or misconfigured; JSON fallback remains a development safety net only.
- Added a protected `POST /api/revalidate` endpoint for operational CMS/script updates that happen outside a live Next request context, allowing authorized invalidation of `irhal-city` and `irhal-city-nav` plus specific paths without requiring a code deployment.
- Public portal performance pass: production Payload city/nav data is cached through Next's data cache with 5-second development freshness and on-demand tag/path revalidation. City/nav reads are request-deduped with React cache, and homepage Karachi warmup now runs through Next `after()` after the response instead of competing with the initial homepage render.
- Homepage mobile LCP tuning now follows the Next.js 16 image contract: the Arabic/default homepage preloads only the true hero image, serves that hero at an allowlisted `q=65`, removes the logo from Next image optimization/preload competition, narrows image width candidates, raises optimized-image cache TTL to 24 hours, and prevents the search typeahead from issuing an empty `/api/search` request during initial page load. Verified in production-mode HTML that there is one image preload, no startup search API URL, no optimized logo URL, and the hero srcset uses `q=65`.
- Public search/typeahead is now protected against cold-cache and client-navigation failures. The search document index is cached through Next's data cache with the `irhal-city-search:documents` tag, API responses emit CDN-friendly cache headers, one-character typeahead input returns a fast empty result instead of building the full search graph, client requests are debounced and aborted after 4 seconds, and search index/API failures degrade to empty results instead of crashing the route. Local verification: one-character API response returns 200 without search build, first real query warms the index, repeated query returns in 27ms, and the production build passes.
- Guide item display order now favors editorially complete records across homepage rails, section grids, related rails, search-derived item lists, and Arabic pages: items with real Payload/R2 media or galleries rank before placeholder-only records, then newer CMS updates rank ahead of older content while preserving existing order for ties. This keeps newly edited/photo-backed entries visible first instead of leading city sections with sparse placeholder cards.
- Search suggestions now collapse generated guide-article duplicates when a real guide item page represents the same entity, and exact duplicate guide-item titles are collapsed to the highest-ranked result. This prevents cases like Expo Centre appearing once as an imported article with an unrelated city hero image and again as the actual place page.
- Payload admin LTR stability fix: `/admin` is now explicitly routed through the locale proxy as English/LTR and the client-side document locale synchronizer treats admin URLs as English, preventing the public Arabic default from leaking into the Payload dashboard. Verified that the admin page reloads with `<html lang="en" dir="ltr">`; Payload packages are already on current npm version `3.85.0`.
- Payload admin/public route isolation fix: public pages now live under the `(frontend)` route group with their own root layout, while `/admin` keeps Payload's full document root layout and CSS. This removes the nested `<html>/<body>` hydration hazard and prevents Payload admin global resets from persisting into the public site after browser-back navigation. Production-browser QA confirms `/ar/city/karachi` → `/admin/login` → Back keeps the logo at `77×20`, Arabic heading font as Cairo, document locale as `lang="ar" dir="rtl"`, and zero nested document nodes.
- Karachi places media enrichment has a production repeatable ingestion script at `scripts/enrich-karachi-place-media.ts`. It prioritizes existing Irhal legacy source pages, extracts their page images, pre-compresses to WebP before Payload/R2 upload, and then falls back to Wikimedia/Commons-backed image metadata only when explicit license/attribution and Karachi/Thatta/Makli location checks pass. This run linked 31 additional Karachi place photos to Payload guide items (media IDs 9-39), moving Karachi place image coverage from 6/110 to 37/110. Irhal-owned images are marked `approved`; Creative Commons/public-domain candidates are linked but marked `draft` with attribution/source/license notes for editorial review. A run report is written to `tmp/karachi-place-media-report.json`.
- Karachi city cover now uses the wide Irhal Mohatta Palace R2 asset only: `karachi-hero-mohatta-palace.webp` (2400×846, 474,242 bytes) copied from `https://irhal.com/wp-content/uploads/2021/04/mohatta-palace-in-karachi-pakistan-scaled-e1617276596471.jpeg`. The ordinary Mazar/gallery image was removed from the city banner sequence for now, and `heroGallery` is empty so carousel controls stay hidden.
- Targeted Karachi media updates are now handled through a narrow script instead of any reseed path. Empress Market and Hill Park were updated in Payload/R2 with the user-supplied Wikimedia images, approved media metadata, source/license attribution, and only the two `guide_items.image` / `imageAlt` fields were changed; production city/detail caches were revalidated after the CMS update.
- City homepage banners support multiple approved hero cover images when needed. Payload `Cities` has a `heroGallery` array-of-media field, Postgres/Payload migrations add the ordered gallery tables (including versions), the city loader emits an ordered/deduped `heroImageUrls` list with the primary `heroImage` first, and `/city/[city]` renders the banner image layer through `CityHeroCarousel` with accessible previous/next controls, dot indicators, RTL-aware labels, single-image fallback, and RTL `scaleX(-1)` mirroring. City cover images should be wide/panoramic cover assets, not ordinary place photos.
- Arabic city banners mirror the image layer with `scaleX(-1)` when the RTL content panel moves to the right, so the main visual subject is not hidden behind the gray panel. The overlay/content itself remains normally readable and RTL-aligned.
- The Karachi city hero now uses a shorter previous-portal-style panoramic treatment with a curved dark information panel and full city excerpt constrained inside the panel.
- Irhal mobile-era brand colors from the supplied screenshots are now first-class Tailwind theme tokens and are used as light-theme accents for guide cards, map signals, category modules, and CTAs.
- The old placeholder-style city category cards and stat boxes have been replaced with a compact one-line Irhal category rail inspired by the original city guide app: places, history, nearby, hotels, restaurants, shopping, city info, transport, festivals, masjids, halal restaurants, and prayer timings. Section tiles now show the same record/article count as their destination pages, live map tiles are labeled as live, and the rail uses shadcn-style `Card`/`Badge` primitives with reference-matched flat tile colors, consistent white labels/icons, and dark count badges for readability.
- The city category rail's halal restaurants shortcut now opens the Food and Restaurants section instead of the broader Islamic Travel overview, so users who choose the restaurant tile land on restaurant cards as expected.
- Generic small-left-icon cards have been removed from the main city guide experience. The former editor tips card section has been removed from the city homepage, while guide-section blocks continue to use Expedia-style image-first cards with titles and descriptions below the media.
- The neighbourhood area on the city homepage now uses a compact one-row carousel with shadcn-style previous/next controls and expands beyond the initial CMS three areas.
- The lower places block is now renamed to “Famous Spots in the City” and uses a daily-shuffled set of place records with compact shadcn-style highlight cards.
- The city homepage now surfaces a Practical City Info carousel from the imported full guide after the main places, hotel, and shopping rails, matching the guide-card format with previous/next controls for weather, exchange rates, emergency numbers, public holidays, language, currency, and links into the source visitor/health sections.
- City guide cards now use a denser GetYourGuide-inspired structure: compact media, dark navy typography, status pills, save icons, stable no-lift hover behavior, and bottom metadata/action rows.
- The final “Irhal city file” guide-section block is condensed into a compact 4-column desktop grid.
- City guide thumbnails now use a consistent compact fixed-height media frame across guide item rails, guide item grids, section cards, neighbourhood cards, and top-place cards. Guide item location/category labels render as compact colored pill tags instead of full-width strips.
- The embedded Google Map has been removed from the city homepage; map access remains available through category tiles, item map links, and detail pages.
- The public website background has been moved away from cream/off-white to a clean white/light-gray system.
- Public city page: `/city/karachi` now uses an editorial destination homepage layout with a large city banner, category navigation, item rails, neighbourhood carousel, and fast facts.
- Dynamic item pages exist for imported places, hotels, restaurants, masjids, shopping entries, tours, family entries, and festivals.
- Karachi guide import exists locally at `src/data/karachi-guide.json`, extracted from the full Karachi enterprise DOCX.
- Payload now has a first-class `countries` collection, and `cities.country` is a Payload relationship instead of plain text.
- Public city routes use a Payload-first data loader when `DATABASE_URL` and `PAYLOAD_SECRET` are configured, with the local Karachi JSON import retained only as a development fallback.
- `.env.local` is configured locally with the Supabase database connection and Payload secret. Secret values are intentionally not committed.
- Payload is the canonical CMS data source and uses the isolated `payload` Postgres schema.
- The old `public.irhal_*` tables have been archived into `legacy_irhal_public`; nothing in the application reads them.
- Payload CMS has been seeded and verified in Supabase: 1 country, 1 city, 6 districts, 72 normalized neighborhoods, 4 anchor listings, 15 guide sections, 411 guide item documents, and 6 itineraries.
- Karachi neighborhoods are now normalized from the 72-row neighborhood operating guide instead of the old three-row homepage subset. Payload `guide_items` has a first-class `neighborhood` relationship, the seed canonicalizes source `area` values such as `DHA Phase 5`, `Clifton / DHA`, `Burns Road`, and `Kharadar`, and the current Karachi seed maps 332 of 411 guide items across 52 real neighborhood pages. Broad/non-local rows such as months, `Multiple`, `Karachi`, and out-of-city tours remain intentionally unmapped.
- All 72 normalized Karachi neighborhoods now have Arabic CMS translations for `name`, `description`, and `operatingGuide`, seeded from `src/data/karachi-neighborhood-ar.json` into Payload `translations.ar`. The Arabic city homepage neighborhood carousel and neighborhood detail hero read those localized fields, so cards such as Saudabad, Malir Cantonment, and Bahria Town Karachi no longer fall back to English.
- Repeated public cards now use a consistent bordered rounded `Discover` action under the card copy. The shared action is wired into guide item cards, famous-spot cards, neighborhood cards, guide-section cards, practical-info cards, city image cards, listing cards, article cards, and offer cards, with Arabic using `اكتشف`.
- Karachi guide content now has a source-preserving Irhal legacy overlay for 72 matched item records across places, restaurants, festivals, shopping, hotels, masjids, family entries, and exhibition/place records. The original in-house summaries are retained in `importedDetails.original_in_house_summary`, while paraphrased Irhal context, verification notes, source URLs, source modified dates, and available legacy address/coordinate hints are stored alongside each matched item and as separate Payload source rows. Six article sections also carry 16 Irhal legacy article source matches for children in tow, city information/history, visitor information, transportation, health and safety, and Muslim visitor information. These AI-enriched matched records and sections are marked `workflowStatus: review` for editorial approval.
- Karachi legacy archive recovery now has a focused Payload import script at `scripts/import-irhal-karachi-legacy-batch.ts`. The first high-confidence batch recovered three missing owned Irhal Karachi records from `https://irhal.com/?s=karachi&post_type=place`: `Sindbad Amusement Park` (`family`), `Saint Patrick's Cathedral` (`place`), and `M.A. Jinnah Road` (`place`). Each record was published with Gulf-ready Arabic editorial fields, verified coordinates, source rows, SEO metadata, and an Irhal-owned R2 media asset. Generic duplicated masjid rows, category pages, weak hotel/tour records, and already-covered synonyms were intentionally skipped pending editorial/map review.
- Public city reads use a short-lived server cache around the Payload-backed city loader to avoid repeated Supabase/Payload work during navigation.
- Public city reads and header city navigation now also use Next's data cache (`unstable_cache`) around the Payload loaders, reducing repeated database work across serverless requests. Public pages no longer preload every local font weight globally, and the city homepage now renders a smaller first-pass set of below-the-fold rail/neighborhood cards while preserving full directory access through section pages.
- Dev server runs at `http://localhost:3001` because another Next app is using port `3000`.
- Root layout suppresses one-level body hydration warnings so browser-extension attributes such as `cz-shortcut-listen` do not trigger the Next.js dev overlay.
- Latest verification for this update: `npm run typecheck` passes; `npm run lint` passes; `npm run build` passes; `/api/search?q=mohatta&locale=en` returns Mohatta Palace Museum as the first typed place result; `/api/search?q=kids&locale=en` returns Traveling With Kids/family results; `/api/search?q=كراتشي&locale=ar` returns Arabic city and item results; browser QA confirms homepage typeahead opens with Mohatta results and the selected result navigates to the Mohatta detail page; `/search?q=mohatta` renders the public results page. Arabic font QA confirms `/ar/city/karachi` computes headings to local Cairo and paragraphs to local Noto Sans Arabic.
- Latest health/safety article verification: `npm run seed:karachi-guide`, `npm run typecheck`, and `npm run lint` pass. Supabase Payload data confirms the `health-and-safety` guide section now has first article slug `health-and-safety`, title `Health and Safety`, 5 total articles, and exactly one legacy source row for the Irhal health-and-safety URL.
- Health/safety article cards now derive resilient excerpts from article body blocks. One-word or missing summaries fall back to the first substantive paragraph, and table-only articles such as `useful-emergency-numbers` summarize their first rows so section cards, search, metadata, and seed output no longer render empty text boxes.
- Karachi `Health and Safety` article formatting was upgraded from flat copied paragraphs into a proper guide layout: lead paragraph, `Personal security`, `Emergency numbers`, `Crowds and public gatherings`, `Health`, hospital subheadings, scannable bullet lists, and bold inline labels for key contacts/addresses/phones.
- The Karachi homepage Practical City Info carousel now includes a first-screen `Health and Safety` card linking directly to `/city/karachi/section/health-and-safety/health-and-safety`, with Arabic copy/link support at `/ar/city/karachi/section/health-and-safety/health-and-safety`. Emergency Numbers remains as a separate practical card.

- Arabic localization "quick wins" + a content-translation proof landed (register is editorial **Modern Standard Arabic / فصحى** — the standard for published Gulf newspaper/editorial copy; spoken dialect is intentionally not used for published content). (1) An Arabic homepage now exists at `/ar` (`(frontend)/ar/page.tsx` rendering a locale-aware `HomeContent`), so the header EN⇄العربية toggle works site-wide instead of 404-ing at the root. (2) `pageMetadata` now emits `hreflang` alternates (`en`, `ar`, `x-default`) for every page. (3) The Arabic font fix was generalized earlier so `PageShell` applies Cairo/Noto across all `/ar` pages. (4) A translation framework was added: repo-managed `src/data/karachi-ar.json` (CMS `translations` still take precedence) feeds Arabic item fields through `withTranslations`/`localizeGuideItem`, including a localized Overview (`translations.ar.overview`) and address rendered by `GuideItemDetail`, plus localized city fast facts and item-detail breadcrumb/section titles. (5) Proof complete: the **Mohatta Palace** detail page (`/ar/city/karachi/place/mohatta-palace-museum`) is end-to-end Arabic — title, eyebrow, lede, quick facts, full multi-paragraph Overview, address, and JSON-LD — with zero English leakage in the main content, and the `/ar` homepage is fully Arabic. Remaining for full coverage: editorial Arabic for the other ~409 items + 15 sections + article bodies, plus per-locale page titles for `/ar` routes (currently reuse English `generateMetadata`).
- The UI typography was modernized to a **Poppins (headings) + Inter (body)** pairing, replacing the earlier Geist font. (TripAdvisor's own "Trip Sans" is a proprietary Colophon Foundry typeface licensed to Tripadvisor/partners only and cannot be used here, so this is a free/OFL, commercially-licensed direction in the same modern spirit.) Both are self-hosted via `next/font/google` (Inter variable; Poppins weights 400–900 to cover heavy headings). Wiring: `layout.tsx` exposes `--font-inter` and `--font-poppins`; `globals.css` sets the body/`--font-sans` token to Inter, adds an `h1–h6` rule (and `--font-heading` token) for Poppins, and keeps the self-hosted Dubai font for Arabic with a scoped `.font-arabic :is(h1…h6)` rule so Arabic headings stay in Dubai. Geist Mono retained for `--font-mono`. Verified computed `font-family: Poppins…` on h1/h2 and `Inter…` on body. Arabic typography was switched to **Cairo (headings) + Noto Sans Arabic (body)** and the self-hosted Dubai font was removed entirely (localFont loader, `--font-arabic-sans`, and `public/fonts/dubai/` deleted): `layout.tsx` loads `--font-cairo` and `--font-noto-arabic` (Arabic subsets), `globals.css` points `--font-arabic`/`.font-arabic` to Noto Sans Arabic and the scoped `.font-arabic :is(h1…h6)` rule to Cairo. Verified on `/ar/city/karachi`: h1 resolves to Cairo and body to Noto Sans Arabic with correct RTL glyphs.
- The item detail hero is now a simple image gallery/carousel (`ImageGallery` client component) with prev/next controls and dot indicators, shown only when an item has 2+ images, and with the previous drop shadow removed (flat, rounded). A `gallery` array-of-media field was added to the Payload `GuideItems` collection so editors can attach multiple R2-backed images per item in Admin; the city loader reads the primary image plus the gallery into an ordered `guideItemGalleries` map, and `getGuideItemImages` returns the full list. Mohatta Palace was given a two-image gallery (its place photo + the hero photo, both genuine Mohatta images) to demonstrate prev/next.
- Guide-item imagery is sourced from Payload Media (Cloudflare R2), not local photos. The Payload city loader (`city-source.ts`) reads linked `image` and `gallery` media, filters replacement-needed/archived/tiny assets, and exposes `cmsImageUrl`/`galleryUrls` to public cards and detail pages. `getGuideItemImage` prefers R2 media and falls back only to the per-kind SVG placeholder when no acceptable CMS media is linked. Uploading or replacing media in Payload Admin surfaces automatically with no code change. `next.config.ts` already allows the `**.r2.dev` host for `next/image`.
- City guide UI integrity and scalability pass: the misleading always-on "Verified" badge on guide cards is now gated on real `geoStatus === "verified"` (guide item cards and the homepage Famous Spots cards), so unverified imported records no longer claim verification. Karachi neighbourhood copy is still localized/presented through the shared presentation helper, but city hero and guide-item photos no longer use hard-coded local curated images on Payload-backed public pages.
- The full original in-house written content for the 71 enriched Karachi items has been migrated verbatim from the legacy irhal.com pages (using each item's stored `sourceUrl`) into `src/data/karachi-original-content.json` (paragraphs + original address line). Item detail pages now render this preserved content as an "Overview" section (with an Address line where available), and guide cards/rails/grids lead with the original first paragraph for density. Masjid records, whose legacy pages held only addresses, show the address. This supersedes the earlier interim paraphrased summaries (`karachi-legacy-descriptions.json`), which were removed. No "Irhal"/third-party source attribution is shown, since this portal is the new Irhal. Also hardened the detail "Helpful details" filter to hide internal `legacy_irhal_source_address`/`legacy_irhal_source_location` keys that were previously leaking onto masjid/shopping/hotel pages.
- The Arabic city hero no longer mirrors (`scale-x-[-1]`) and left-crops the photo, which previously distorted the real Karachi landmark and showed garden/greenery instead of the building. Both locales now use the same unmirrored `object-cover object-center` treatment, so Arabic shows the real Karachi hero image identical to English. The hero image source itself was unchanged by the earlier presentation refactor.
- Guide item cards (rails, section grids, and the homepage Famous Spots cards) are denser: the reserved blank title height and large title/description gap were removed, the description now allows up to four lines, and a teal category/price meta line (e.g. "FOOD STREET · BUDGET") was added under the title for additional real content. Note: imported guide descriptions are intentionally concise, so true 4–5 line blurbs would require richer source or AI-enriched copy rather than larger line clamps alone.
- Inner city-guide pages are now localized for Arabic: the section landing page and the guide item detail page render Arabic chrome (eyebrows, intros, buttons, geo-status, "before you go" tips), localize guide items, use locale-prefixed internal links, and replace the CMS-flavoured "N records" label with consumer nouns (e.g. "110 places" / "110 أماكن"). Section headings/summaries use the Arabic section dictionary. The item detail page also drops the redundant title-as-detail row and removes the hardcoded Karachi-only "before you go" tip in favour of a city-aware string. Inner pages were de-brutalised (red eyebrow + smaller heading, no `font-mono uppercase` accents) to match the homepage and new header.
- Karachi Arabic editorial coverage has been expanded beyond the Mohatta Palace proof: `src/data/karachi-ar.json` now contains Modern Standard Arabic translations for all 411 Karachi guide items plus 96 guide articles across 15 sections. The approved Mohatta entry was preserved, while remaining item titles, summaries, categories, areas, image alt text, and available long-form overviews/addresses were localized so Arabic item detail pages no longer fall back to English overview content. Arabic section article pages now consume localized article titles, summaries, and body blocks through `localizeGuideArticle`, and Arabic section/article cards use the localized article payload. QA checks confirmed valid JSON, 411 item entries, 96 article entries, no visible Latin text outside URLs, and `200 OK` smoke responses for Arabic item, section, and article routes.
- Practical city-info detail articles now restore their structured source tables instead of rendering as thin paragraph-only pages. `getGuideArticlesForSection` keeps inline table blocks and also attaches canonical tables from `fullGuide.tables` for Payload-backed articles whose CMS block stream is missing table blocks: `when-to-go` now shows the 12-month climate table, `fast-facts` shows the fast facts table, `public-holidays` shows holiday rows, and `useful-emergency-numbers` shows emergency contacts. Arabic article localization preserves these table blocks while replacing paragraph copy. Verified live `200 OK` routes for `/city/karachi/section/visitor-information/when-to-go`, `/public-holidays`, `/fast-facts`, and `/city/karachi/section/health-and-safety/useful-emergency-numbers`, with expected table rows present.
- City guide article detail pages now match the richer item-detail information architecture: a full-width article hero, a two-column content body, a sticky sidebar with guide-section navigation, other articles from the same section, itinerary/map CTAs, plus below-page discovery grids for more articles and other city guide sections. The layout is localized for English and Arabic, uses the existing shadcn-style card/sidebar primitives, and keeps restored structured tables inside the article body. Verified `When to go` in English and Arabic renders the table, sidebar guide nav, related article links, and below-section discovery.
- City guide article detail pages were refined after QA: the duplicated hero summary is removed from the article body when it matches the first paragraph, the generic "Explore the guide" sidebar index was removed, and the bottom "More guide sections" card grid was replaced with destination/item rails for top places, hotels, shopping, and restaurants. The sidebar now focuses on same-section article recommendations plus planning/map CTAs, while below-page discovery matches the item-detail pattern. Verified the `When to go` article body starts directly with the climate table and that the visible page no longer contains "Explore the guide" or "More Karachi guide sections".
- The About Us pages now use dynamic image-led city cards instead of plain city link rows. `getCityNavItems()` carries each city's CMS/local hero image URL, and `/about` plus `/ar/about` render full-bleed city cards with city and country anchored at the bottom-left/bottom-right over the photo, so the module will scale automatically as more Payload cities are added.
- The About Us sidebar now restores the legacy Irhal mobile-app promo image (`public/images/irhal_app.png`) above the destination city cards, with localized English/Arabic download copy.
- English portal typography now uses the provided local Trip Sans font files from `public/fonts/Trip-Sans-Font/` through `next/font/local` for both body and headings. Arabic pages remain explicitly scoped to Noto Sans Arabic for body text and Cairo for headings. The About Us app promo no longer duplicates the download text outside the image.
- Guide item/end pages have been redesigned away from the sparse split hero into a denser GetYourGuide-style detail layout: title/meta/actions at the top, large media block and sticky planning sidebar in the first viewport, practical highlight tiles, overview content on the left, quick facts and related item discovery on the right, plus the existing cross-discovery rails below. Verified on `/city/karachi/place/paradise-point`.
- The separate "Related guides" article-card block was removed from guide item detail pages; item pages now rely on the sidebar discovery and item recommendation rails without the extra article-card section.
- Internal "CMS media placeholder" labels were removed from the Karachi fallback SVG artwork so placeholder images no longer expose CMS implementation language on public pages.
- Guide item media now opens into a full-screen image viewer/lightbox on click, with next/previous controls, keyboard arrow support, close action, and thumbnails when multiple images exist. The item template also suppresses the intro paragraph when it duplicates the first Overview paragraph or the Overview fallback, removing repeated text below the image area.
- Removed redundant “Explore” CTAs from the city hero and the global header. The header right side now shows the language switch followed by a Login / Sign in link to the Payload admin login.
- Header navigation was tightened after visual review: desktop nav labels now render in darker ink with stronger weight, tablet switches to the mobile menu sooner to avoid crowding, and the public nav includes Islamic Travel, Travel Articles, and Special Offers in both desktop and mobile menus. Travel Articles and Special Offers now have lightweight EN/AR landing pages so the header links do not route to empty states.
- Public guide-item ordering is now stable: Payload guide-items are fetched with an explicit `id` sort, and the public guide resolver no longer reorders cards/rails by media presence or `updatedAt`. Editing a guide item, attaching a photo, or updating CMS metadata should not silently reshuffle Karachi guide sections.
- Karachi itineraries are now a real public planning feature rather than a single starter card: `/en/city/karachi/itineraries` renders an image-led itinerary hub with 6 CMS-published routes, and each route has a dedicated EN/AR detail page at `/city/karachi/itineraries/[itinerary]` with a hero, route facts, day-by-day stop timelines, internal detail links, map links, JSON-LD, and a print-driven PDF export button.
- Itinerary detail stop timelines render compact stop thumbnails from approved Payload/R2 media or the clean per-kind placeholder, while keeping the route cards focused on verified title, summary, detail links, and map links. The itinerary hero image uses the first available route stop or city image.
- Wrong item-level media was removed for `dolmen-mall-clifton`, `clifton-beach-horse-camel-rides`, `boat-basin-food-street`, `atrium-mall`, and `ocean-mall`: the bad Payload image relationships on the affected records were cleared, and related cards now show the standard placeholder until proper editorial R2 media is assigned in Payload. There is no hard-coded slug suppression, so a future correct CMS image will appear automatically.
- Public guide-item media now has a card-quality gate: guide-item media marked `needs-replacement`/`archived`, under 480×320, or under 15 KB is ignored by the city loader. Ten weak restaurant media attachments were detached from public records and marked replacement-needed (`port-grand-restaurants`, `sattar-buksh`, `bbq-tonight`, `bundu-khan`, `cafe-flo`, `chinatown-karachi`, `food-centre`, `javed-nihari`, `student-biryani`, `usmania-restaurant`) because the imported legacy thumbnails were wrong or too blurred for modern cards.
- Guide item cards now show the existing clean per-kind SVG placeholder whenever an item has no acceptable editorial media. This keeps all section grids and rails visually structured without reintroducing low-quality or blurred imported photos.
- Guide item media fallback was tightened after the Payload-only media pass: approved repo-owned Karachi item photos are restored only for vetted records (`Mohatta Palace Museum`, `Mazar-e-Quaid`, `Frere Hall`, and `Karachi Port Trust Building`) while all other missing media still uses the per-kind placeholder until an editor attaches proper Payload/R2 media. Itinerary stop thumbnails now call the same shared `getGuideItemImage` resolver as item pages/cards so image behavior is consistent across the site.
- Karachi itinerary/media reseed safety fix: `scripts/seed-karachi-guide.ts` now relinks non-slug media filenames for editorially fixed stops such as Merewether Clock Tower, National Museum of Pakistan, and Memon Masjid, and preserves the improved `Historic landmark` category for Merewether. CMS-backed guide item rendering now falls back to repo long-form original content when a reseeded CMS row has no rich-text body, preventing thin/empty detail pages after local reseeds.
- The Karachi baseline seed is now guarded against accidental CMS data loss. `npm run seed:karachi-guide` refuses to run when Karachi already has child records in Payload, because that baseline path deletes/recreates districts, neighborhoods, listings, sections, guide items, and itineraries. Intentional local resets must use the explicit destructive command `npm run seed:karachi-guide:reset`; live/content updates should use targeted migration or update scripts.
- Karachi itinerary public pages treat Payload itinerary rows as canonical for title, summary, audience, duration, and day labels, but merge local fallback route stops and planning notes when the CMS day payload is incomplete. This prevents empty live itinerary pages while preserving Payload as the editable source; the follow-up CMS task is to store the full route stops and planning copy directly in Payload.
- CMS-backed guide item pages now reapply the preserved Irhal original-content overlay when a Payload guide item has no rich-text body, so edited CMS media/title/category fields remain canonical while long-form Overview copy does not collapse to a one-line summary.
- Arabic guide item and article pages now reapply repo-managed Gulf-editorial Arabic fallback translations when Payload does not yet contain Arabic fields, with Payload Arabic still taking precedence where present. This restores Arabic titles, summaries, overviews, addresses, section article text, and RTL detail-page copy on Payload-backed Karachi pages.
- Homepage hero panel copy now uses a shorter one-line Islamic travel guide message for top places, hotels, halal food, tours, and more.
- Restored old-Irhal-style traveler facts inside the city hero: Currency, Language, and Best time to visit, with Arabic labels and Karachi-specific values (Rupee / Urdu-English / November to February).
- The public homepage has been rebuilt into a modern portal entry instead of the earlier map/features layout: a shorter editorial photographic hero, SEO-focused traveler copy, search-style city CTA, available-city chips, and a dynamic visual city picker fed by `getCityNavItems()`. Arabic `/ar` uses the same template with localized copy and RTL direction.
- The homepage city picker was refined into compact portrait destination cards inspired by the old Irhal trending-destination strip: smaller section heading, narrower/taller image tiles, and no separate “View guide” button under each tile.
- Homepage hero copy was simplified: removed the eyebrow label, increased the editorial image banner height, and changed the panel message to position Irhal as an Islamic travel partner.
- Homepage layout tuned after visual QA: hero height increased, hero copy/search panel moved upward toward vertical center, and city picker portrait cards shortened to a less dominant 3:4 destination-tile ratio.
- Homepage hero search was simplified to a true search-style control with placeholder text and a plain Search button, removing the confusing “Open Karachi guide” CTA from the search bar.
- Public search Phase 1 is now implemented as a Payload-backed, in-house ranked search experience. `/api/search` builds a short-lived searchable index from Payload-first city data (cities, neighborhoods, guide sections, guide articles, and guide items), adds typo-tolerant title matching and traveler synonyms (food/restaurants, mosque/masjid/prayer, kids/family, shopping/markets, etc.), and returns typed results with labels, descriptions, images, city context, and public URLs. The homepage hero uses the reusable `SiteSearchBox` with live typeahead dropdown results, keyboard support, and direct navigation to result pages. `/search` and `/ar/search` provide full results pages with localized copy and RTL-aware Arabic output.
- Production build stability: remaining `next/font/google` usage was removed because Turbopack production build failed resolving Google font internals. English typography remains self-hosted Trip Sans, monospace now uses the provided local Trip Sans Mono file, and Arabic typography is restored through self-hosted Cairo headings and self-hosted Noto Sans Arabic body text loaded with `next/font/local`.
- Neighborhood detail pages (`/city/[city]/neighborhood/[area]` and `/ar/city/[city]/neighborhood/[area]`) were redesigned from the old disconnected map/listings utility page into a full Irhal portal subpage: image-led area hero, localized area copy, city/district/cluster facts, area map CTA, sticky map/sidebar, live locators, mapped place cards, and a content-led “In this neighborhood” index. The page now groups every matching guide item by category/section (places, restaurants, hotels, shopping, etc.) with counts, anchor navigation, and full item grids instead of a small related teaser. Arabic routes reuse the same template with RTL layout and localized labels.

## Requirement Coverage

| Requirement Area                                       | Status      | Notes                                                                                                                                                                                                                       |
| ------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Map-first city guide architecture                      | In Progress | Public routes and map panels exist. Full provider-backed live map enrichment is still pending API keys and enrichment workflow.                                                                                             |
| Dynamic city page template                             | Completed   | `/city/[city]` is reusable for future cities and driven by city data plus guide item transforms.                                                                                                                            |
| Separate document/page per place/restaurant/hotel/etc. | Completed   | Public pages exist, Payload collections exist, and 411 Karachi guide item documents are seeded into Payload CMS. Remaining work is media/geo enrichment, not the document model.                                            |
| Karachi enterprise guide import                        | Completed   | Imported all required guide sections and all detected tables from the attached Karachi DOCX into structured JSON.                                                                                                           |
| Global city import format                              | In Progress | Import format and extractor exist. Needs normalization rules, validation reports, and bulk import pipeline for all world cities.                                                                                            |
| Payload CMS/Auth/Admin                                 | In Progress | Collections, admin route, auth, Payload API route, and Payload-first frontend loader are implemented against Supabase. Editor user creation and workflow hardening remain.                                                  |
| PostgreSQL portability                                 | Completed   | Supabase migrations are stored in repo and use portable PostgreSQL conventions where possible. Legacy public tables are archived; Payload schema is canonical.                                                              |
| Country/city geography model                           | Completed   | Payload has `countries`; cities relate to countries. Pakistan and Karachi are seeded in the Payload schema.                                                                                                                 |
| PostGIS                                                | Completed   | Database migration includes PostGIS extension and geo-capable schema.                                                                                                                                                       |
| pgvector                                               | Completed   | Database migration includes vector extension for future embeddings.                                                                                                                                                         |
| Cloudflare R2 / Cloudflare Images                      | In Progress | Payload Media is actively storing uploads in Cloudflare R2 through `@payloadcms/storage-s3`. Cloudflare Images remains pending.                                                                                             |
| Search Phase 1                                         | Completed   | Payload search plugin/config is scaffolded, CMS data exists, and the public homepage/API/results UI now search the Payload-first content graph with typed, ranked results.                                                  |
| Advanced search Phase 2                                | Not Started | Meilisearch/Typesense intentionally deferred.                                                                                                                                                                               |
| AI agent architecture                                  | In Progress | `AGENTS.md` defines agents, rules, input formats, output formats, and statuses. Actual provider-backed execution and schema validation remain.                                                                              |
| AI assistant API                                       | In Progress | JSON-only route scaffold exists. Model provider integration awaits API keys.                                                                                                                                                |
| Muslim travel layer                                    | In Progress | Islamic travel page, masjid pages, halal-aware content model, and tags exist. Women prayer area verification and prayer-time awareness remain.                                                                              |
| SEO foundation                                         | In Progress | Metadata helpers, canonical paths, JSON-LD, breadcrumbs, SSR pages, internal links, sitemap, robots, and search `noindex` handling exist. Dynamic OG images, duplicate-content QA, and production domain validation remain. |
| Multi-language readiness                               | In Progress | Locale fields, localized routing, translated content workflow, and English/Arabic hreflang are in place. Deeper Arabic editorial templates and cross-city translation operations remain.                                    |
| Vercel hosting                                         | Blocked     | Waiting for Vercel project/token and production environment variables.                                                                                                                                                      |

## Feature Tracker

| Feature                            | Status      | Owner | Priority | Dependencies                                                         | Notes                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------------- | ----------- | ----- | -------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Next.js App Router Foundation      | Completed   | Codex | High     | Node, npm                                                            | Next.js 16, TypeScript, Tailwind, lint, typecheck, and build scripts are configured.                                                                                                                                                                                                                                                                           |
| Irhal UI Design System Foundation  | Completed   | Codex | High     | Tailwind v4, shadcn-style primitives                                 | Added owned shadcn-style config, `cn` helper, and Irhal-branded `Button`, `Badge`, and `Card` primitives with custom travel tokens.                                                                                                                                                                                                                            |
| Public Travel Homepage Style       | Completed   | Codex | High     | City data, guide item data, image asset, Irhal UI foundation         | `/city/karachi` now has a large city banner, planning cards, guide cards, visual item rails, and map intelligence lower on the page using the custom Irhal identity.                                                                                                                                                                                           |
| Payload CMS Foundation             | In Progress | Codex | High     | `DATABASE_URL`, `PAYLOAD_SECRET`                                     | Payload config, auth, collections, admin route, `/payload-api` route, and Supabase-backed local env are configured. Editor user creation and production workflow hardening remain.                                                                                                                                                                             |
| Payload Collections                | In Progress | Codex | High     | Payload CMS foundation                                               | Countries, cities, guide sections, guide items, districts, neighborhoods, listings, itineraries, AI jobs, media, users, and update logs are modeled in the canonical Payload schema. Karachi baseline data is seeded; editor workflow testing remains.                                                                                                         |
| Countries Collection               | Completed   | Codex | High     | Payload CMS foundation                                               | Added first-class countries collection and changed cities to relate to countries. Pakistan is included in the seed workflow.                                                                                                                                                                                                                                   |
| Supabase/PostGIS/pgvector Schema   | Completed   | Codex | High     | Supabase Irhal MCP                                                   | Migrations applied through the Irhal Supabase MCP. Security advisor was clean after hardening.                                                                                                                                                                                                                                                                 |
| Legacy Public Irhal Tables         | Completed   | Codex | Medium   | Payload canonical schema decision                                    | Archived `public.irhal_*` tables into `legacy_irhal_public` so Supabase public schema no longer appears to be the active app data model.                                                                                                                                                                                                                       |
| City Page System                   | Completed   | Codex | High     | City data model                                                      | `/city/{city-name}` route exists with SSR metadata, JSON-LD, reusable destination template, internal links, and public travel design.                                                                                                                                                                                                                          |
| URL Structure                      | In Progress | Codex | High     | Dynamic routes                                                       | Required routes exist for city, neighborhoods, places, hotels, restaurants, Islamic travel, itineraries, shopping, tours, masjids, family, and festivals. Needs final production URL QA and redirects/canonical domain setup.                                                                                                                                  |
| Full Karachi City Guide Import     | Completed   | Codex | High     | DOCX extractor                                                       | Imported 15 required sections and 17 tables from `Karachi_Pakistan_City_Guide_Irhal_Enterprise_FULL_2026.docx`.                                                                                                                                                                                                                                                |
| City Import Format Documentation   | Completed   | Codex | High     | DOCX extraction                                                      | `docs/CITY_IMPORT_FORMAT.md` documents the import structure and directory-to-document model.                                                                                                                                                                                                                                                                   |
| City Section Pages                 | Completed   | Codex | High     | Full guide import                                                    | `/city/{city-name}/section/{section}` renders section landing pages with cards and separate item/article links.                                                                                                                                                                                                                                                |
| Section Article Pages              | Completed   | Codex | Medium   | Guide section headings                                               | `/city/{city-name}/section/{section}/{article}` exists for section-level editorial documents.                                                                                                                                                                                                                                                                  |
| Payload-first Frontend Data Source | Completed   | Codex | High     | Payload CMS env vars                                                 | Public routes attempt Payload first and fall back to local JSON only when database env vars are missing or unavailable. Verified with Supabase-backed local env.                                                                                                                                                                                               |
| City Frontend Read Cache           | Completed   | Codex | High     | Payload-first frontend data source                                   | Added in-process city cache and homepage preload for faster `/city/karachi` navigation. Local verification improved repeated city response time to roughly 0.18s.                                                                                                                                                                                              |
| Guide Item Documents               | Completed   | Codex | High     | Payload GuideItems collection, seed script                           | Public item pages exist and 411 Karachi guide items are seeded as Payload CMS documents. Seventy-two matched Karachi item records now preserve original in-house summaries while adding paraphrased Irhal legacy context, verification notes, address/coordinate hints where available, and source rows.                                                       |
| Places Directory                   | In Progress | Codex | High     | Guide item model, map links                                          | `/city/{city-name}/place/{slug}` exists for imported attractions. Many records still need verified coordinates/images/provider IDs.                                                                                                                                                                                                                            |
| Hotel Directory                    | In Progress | Codex | High     | Guide item model, CMS seed                                           | `/city/{city-name}/hotel/{slug}` exists. Needs live hotel enrichment, affiliate fields, photos, and verification.                                                                                                                                                                                                                                              |
| Restaurant Directory               | In Progress | Codex | High     | Guide item model, halal tags                                         | `/city/{city-name}/restaurant/{slug}` exists. Needs halal/source verification and live map enrichment.                                                                                                                                                                                                                                                         |
| Shopping Directory                 | In Progress | Codex | High     | Guide item model                                                     | `/city/{city-name}/shopping/{slug}` exists. Needs provider enrichment, photos, and CMS seeding.                                                                                                                                                                                                                                                                |
| Tours Directory                    | In Progress | Codex | Medium   | Guide item model                                                     | `/city/{city-name}/tour/{slug}` exists. Needs vetted suppliers and commercial workflow.                                                                                                                                                                                                                                                                        |
| Festivals Directory                | In Progress | Codex | Medium   | Guide item model                                                     | `/city/{city-name}/festival/{slug}` exists. Needs recurring date model and update cadence.                                                                                                                                                                                                                                                                     |
| Family/Children Directory          | In Progress | Codex | Medium   | Guide item model                                                     | `/city/{city-name}/family/{slug}` exists. Needs family suitability QA and image enrichment.                                                                                                                                                                                                                                                                    |
| Neighborhood System                | In Progress | Codex | High     | District/zone model, geo data                                        | Neighborhood pages now use the same polished portal template in English and Arabic, with area hero, map/sidebar, live locators, mapped listings, and related city-guide picks. Needs complete district/zone/cluster model in CMS for all imported neighborhoods.                                                                                               |
| Live Map Layer                     | Blocked     | Codex | High     | Google Maps or Mapbox API key                                        | Static map-style panels and map URLs exist. Real provider search, place enrichment, and live locators await API keys.                                                                                                                                                                                                                                          |
| Geo Validation Workflow            | In Progress | Codex | High     | Map provider integration                                             | Schema has geo fields and `geoStatus`. Imported guide items currently mark many records as provider-enrichment required.                                                                                                                                                                                                                                       |
| Masjid Directory                   | In Progress | Codex | High     | Guide item model, Muslim travel layer                                | `/city/{city-name}/masjid/{slug}` exists. Needs women prayer area verification and provider-enriched coordinates.                                                                                                                                                                                                                                              |
| Islamic Travel Module              | In Progress | Codex | High     | Masjids, halal restaurants, Islamic landmarks                        | `/city/{city-name}/islamic-travel` route exists. Prayer timing awareness remains future work.                                                                                                                                                                                                                                                                  |
| Itineraries                        | In Progress | Codex | Medium   | Route-aware listings, PDF export, AI output schema                   | `/city/{city-name}/itineraries` now has 6 Karachi plans plus dedicated itinerary detail pages with stop thumbnails, trip-planning overview cards, day-by-day logistics, map/detail links, and print-to-PDF export. Needs AI-generated route optimization and saved itinerary support.                                                                          |
| AI Travel Assistant API            | In Progress | Codex | High     | Approved content, model provider key                                 | `/api/ai/travel-assistant` returns structured JSON pattern. Real model integration awaits provider key.                                                                                                                                                                                                                                                        |
| City Content Generation Agent      | In Progress | Codex | High     | Agent schemas, editorial review workflow                             | Defined in `AGENTS.md`; execution pipeline not implemented yet.                                                                                                                                                                                                                                                                                                |
| SEO Optimization Agent             | In Progress | Codex | High     | SEO snapshots, schema validation                                     | Defined in `AGENTS.md`; automated validation/report persistence remains.                                                                                                                                                                                                                                                                                       |
| Data Validation Agent              | In Progress | Codex | High     | Source logs, validation schema                                       | Defined in `AGENTS.md`; automated blocking workflow remains.                                                                                                                                                                                                                                                                                                   |
| Map/Place Enrichment Agent         | Blocked     | Codex | High     | Google Maps or Mapbox API key                                        | Defined in `AGENTS.md`; cannot enrich provider places without API key.                                                                                                                                                                                                                                                                                         |
| Travel Itinerary Generator         | In Progress | Codex | Medium   | AI provider key, route-aware data                                    | Defined in `AGENTS.md`; provider-backed generation remains.                                                                                                                                                                                                                                                                                                    |
| Muslim Travel Assistant            | In Progress | Codex | High     | AI provider key, masjid/halal verification                           | Defined in `AGENTS.md`; live assistant behavior remains.                                                                                                                                                                                                                                                                                                       |
| Content Update Agent               | In Progress | Codex | Medium   | Update logs, freshness scans                                         | Update log model exists. Scheduled monitoring remains.                                                                                                                                                                                                                                                                                                         |
| SEO Engine                         | In Progress | Codex | High     | Metadata helpers, schema helpers                                     | Page metadata, canonical helpers, localized JSON-LD URLs, breadcrumbs, sitemap, robots, search `noindex`, and SSR are in place. OG image generation and production domain canonical QA remain.                                                                                                                                                                 |
| Schema.org JSON-LD                 | In Progress | Codex | High     | SEO helpers                                                          | City and breadcrumb JSON-LD exist. Needs richer per-item schema for all guide item types.                                                                                                                                                                                                                                                                      |
| Internal Linking                   | In Progress | Codex | High     | City/item routes                                                     | City page, section pages, articles, guide item details, listing details, neighborhoods, Islamic travel, itineraries, and news now use a shared visible breadcrumb bar across English and Arabic public routes. Section directory pages no longer duplicate the main section title. Needs related-items logic and cross-city links.                             |
| OpenGraph/Twitter Cards            | In Progress | Codex | Medium   | Metadata helpers, hero/media assets                                  | Basic metadata exists. Needs dynamic social images per city/item.                                                                                                                                                                                                                                                                                              |
| Search Phase 1                     | Completed   | Codex | Medium   | Payload Search Plugin, CMS data                                      | `/api/search`, homepage typeahead, `/search`, and `/ar/search` are implemented against the Payload-first content graph with ranked city/place/article/section/neighborhood results.                                                                                                                                                                            |
| Search Phase 2                     | Not Started | Codex | Low      | Meilisearch or Typesense decision                                    | Deferred until scale requires advanced search.                                                                                                                                                                                                                                                                                                                 |
| Media Storage                      | In Progress | Codex | High     | Cloudflare R2 credentials                                            | Payload Media is configured and verified for Cloudflare R2, and Media/Neighborhood/Listings image fields exist in the Payload schema.                                                                                                                                                                                                                          |
| Guide Item Image Discovery         | In Progress | Codex | High     | Unsplash API key, editorial review                                   | Payload Admin guide items now have a Media-tab image discovery tool that searches vetted Unsplash candidates, imports a selected asset through the normal Payload Media/R2 pipeline, records attribution/source/license-review metadata, and sets the current primary image field for editor save/publish.                                                     |
| Image Optimization/CDN             | In Progress | Codex | High     | Cloudflare Images credentials                                        | Payload now compresses uploaded images into capped WebP originals and derivatives before R2 storage. Next image optimization works locally. Cloudflare Images integration awaits keys.                                                                                                                                                                         |
| Production Deployment              | Blocked     | Codex | High     | Vercel credentials, env vars                                         | Build succeeds locally. Vercel deployment remains.                                                                                                                                                                                                                                                                                                             |
| Environment/API Keys               | Blocked     | User  | High     | Cloudflare R2, Cloudflare Images, Vercel, Google/Mapbox, AI provider | Supabase and Payload local env are configured. Cloudflare, Vercel, map provider, and AI provider keys are still required for media, deployment, live maps, and live AI.                                                                                                                                                                                        |
| Multi-language Architecture        | In Progress | Codex | Medium   | Locale routing strategy, translation workflow                        | Arabic `/ar/city/{city-name}` route exists with RTL layout, self-hosted Dubai Font, localized homepage/header UI labels, RTL-aware carousel controls, mirrored card badges/actions, Payload `translations` JSON fields, request-aware root `lang`/`dir`, and English/Arabic hreflang/canonical pairs. Need deeper Arabic templates and editorial translations. |
| User Accounts                      | Not Started | Codex | Medium   | Payload Auth, frontend account UI                                    | Payload Auth exists at backend level. Public account flows are future work.                                                                                                                                                                                                                                                                                    |
| Saved Itineraries                  | Not Started | Codex | Medium   | User accounts, itinerary model                                       | Future feature.                                                                                                                                                                                                                                                                                                                                                |
| Reviews System                     | Not Started | Codex | Low      | User accounts, moderation model                                      | Future feature.                                                                                                                                                                                                                                                                                                                                                |
| React Native Mobile App            | Not Started | Codex | Low      | Stable API contracts                                                 | Future feature.                                                                                                                                                                                                                                                                                                                                                |

## Completed Work

- Built the Next.js 16 application foundation with TypeScript and Tailwind.
- Added the owned shadcn-style UI foundation with `components.json`, shared UI primitives, Irhal color tokens, and frontend route/component adoption.
- Added Payload CMS configuration, admin route, collections, auth foundation, and Payload API route.
- Added first-class countries in Payload and linked cities to countries.
- Added a Payload-first frontend data source with local JSON fallback only for missing database env vars.
- Added local `.env.local` configuration for Supabase-backed Payload CMS using the provided database password.
- Made the Payload `payload` schema the canonical source of truth.
- Archived the old `public.irhal_*` tables into `legacy_irhal_public`.
- Added Supabase migrations for the Irhal V4 data foundation, including PostGIS and pgvector.
- Extracted the full Karachi enterprise DOCX into structured JSON.
- Built public routes for the city page, sections, articles, neighborhoods, item detail pages, Islamic travel, and itineraries.
- Reworked the Karachi city main page into a full travel destination homepage rather than a flat CMS/article page.
- Removed the city homepage editor travel tips section so the guide flows from category navigation directly into top places.
- Added an Arabic RTL version of the Karachi city homepage at `/ar/city/karachi`, including Arabic UI labels, Arabic page metadata, route aliases for linked city subsections, and Payload translation storage fields.
- Added full repo-managed Arabic editorial translations for all 411 Karachi guide items and 96 guide articles across the 15 Karachi guide sections, using Modern Standard Arabic suitable for Gulf/Saudi/UAE readers and preserving the approved Mohatta Palace translation.
- Restored source tables on practical city-info article pages so Weather/When To Go, Fast Facts, Public Holidays, and Useful Emergency Numbers display their structured rows after click-through from the homepage cards.
- Localized practical Arabic article tables and generated article fallbacks, including RTL table direction, Arabic table headings, Arabic row values, Arabic sidebar titles, Arabic metadata for Karachi city-guide articles, the September climate row, and Arabic homepage neighborhood carousel copy.
- Reworked Karachi city homepage rail headings and subheadings in English and Arabic to remove internal/CMS-style phrasing such as "separate guide pages" and replace it with traveler-facing editorial copy for places, restaurants, hotels, shopping, highlights, and tours.
- Replaced the Karachi homepage prayer shortcut count with a real prayer-times destination. Added `/city/karachi/prayer-times`, `/ar/city/karachi/prayer-times`, and `/api/prayer-times?city=karachi`, backed by the AlAdhan Prayer Times API with the University of Islamic Sciences, Karachi calculation method, showing today, next prayer, and a monthly timetable in English and Arabic.
- Added the existing Traveling With Kids / `children-in-tow` section to the top Karachi city category rail and as a full homepage discovery rail in English and Arabic so family planning appears with the main city guide shortcuts and the main scroll flow.
- Implemented public search Phase 1: custom ranked search over Payload-first CMS city data, live homepage typeahead, `/api/search`, and localized `/search` + `/ar/search` result pages.
- Removed the loose city fast-facts strip from the bottom of the Karachi city homepage and added a shared dark 5-column Irhal portal footer modeled on travel-directory footers, with Popular News, dynamic Popular Destination, dynamic Shopping, dynamic Hotels, Connect with Irhal, English/Arabic labels, social links, and `© Irhal 2007 - 2026` copyright.
- Removed placeholder/fake Popular News links from the footer so the column remains empty until real news content exists. Added English `/about` and Arabic `/ar/about` About Us pages using the legacy Irhal story copy, with a dynamic city-selection sidebar sourced from Payload city navigation.
- Backfilled latest Payload version rows for all 411 Karachi `guide-items` so the draft-enabled Payload Admin list displays the complete collection instead of only the one manually edited item; updated the Karachi seed script to create guide-item version rows on future reseeds.
- Upgraded guide article detail pages with the same sidebar and below-page discovery pattern used by item detail pages, including same-section article recommendations and cross-section guide links.
- Refined guide article detail pages to remove duplicated lead paragraphs, remove the guide-index sidebar, and replace generic guide-section cards with item rails for places, hotels, shopping, and restaurants.
- Added separate public pages for places, hotels, restaurants, masjids, shopping, tours, festivals, and family entries.
- Added `AGENTS.md` with active AI agent definitions and operating rules.
- Reworked and verified the seed script as a direct SQL/bulk import into the Payload schema with post-seed row-count validation; later guarded it so the default command only bootstraps empty/baseline data and cannot accidentally wipe edited Payload CMS records.
- Seeded Payload CMS with 1 country, 1 city, 6 districts, 72 neighborhoods, 4 listings, 15 guide sections, 411 guide items, and 6 itineraries.
- Added dedicated Karachi itinerary detail pages with route stops, map/detail links, EN/AR route support, structured metadata, and print-to-PDF export.
- Added a source-preserving Irhal legacy update overlay for 72 matched Karachi item records and 16 legacy article source matches across children, city information/history, visitor information, transportation, health and safety, and Muslim visitor information.
- Added a dedicated Karachi `Health and Safety` guide article at `/city/karachi/section/health-and-safety/health-and-safety`, paraphrased from the legacy Irhal source and refreshed with current advisory-aware guidance for street crime, transport, bottled water, heat/monsoon risk, emergency numbers, and major healthcare options. The source attribution is retained in the legacy article update map and the Arabic article translation is included for `/ar/city/karachi/section/health-and-safety/health-and-safety`.
- Promoted Karachi itineraries into the first city-guide discovery position: the homepage shortcut rail now opens `/city/karachi/itineraries` first, and the shared travel-guide section grid uses the Payload itinerary section title/summary while linking to the full itinerary index instead of the older article-section route.
- Added a first-position Karachi homepage itinerary card rail before Top Places, showing each Payload itinerary with route-derived imagery, duration, stop count, audience, and direct itinerary links in English and Arabic. Backfilled Gulf-editorial Arabic itinerary title/summary/audience translations into Payload and wired the city homepage, itinerary index, and itinerary detail pages to use Payload itinerary translations instead of English fallback on Arabic routes.
- Improved Karachi itinerary card imagery so homepage and itinerary-index cards choose distinct route-stop images from Payload/R2 across the itinerary list instead of repeating the first shared stop image on multiple plans.
- Added below-page city-guide section cards to Karachi itinerary detail pages, excluding the current itinerary section and using the shared Payload-backed section grid so route pages continue into places, hotels, restaurants, shopping, transport, family, masjid, and other guide sections before the footer.
- Moved the Arabic itinerary index title/summary into Payload guide-section translations and standardized itinerary duration/stop labels across homepage, itinerary index, and itinerary detail pages with Gulf-ready Arabic wording and Eastern Arabic numerals.
- Backfilled detailed Gulf-editorial Arabic itinerary copy into Payload for all six Karachi itinerary records, including intro, stay/transport/meal planning cards, day themes, day descriptions, logistics notes, and route notes. Itinerary detail pages now merge nested Payload translations for Arabic routes instead of falling back to English nested route copy.
- Added the legacy Irhal "What to do with Children in Tow" roundup as the first Karachi `children-in-tow` family guide item, with source attribution and English/Arabic planning copy, so it appears first in the "Traveling with kids in Karachi" homepage rail and family section.
- Fixed Karachi Zoo media linkage so the existing R2 `karachi-zoo.webp` Payload Media record is used by both the place and family Karachi Zoo guide items; updated the Karachi seed script to restore filename-matched guide item media links during reseeds.
- Added the Karachi guide-item media enrichment workflow for all item kinds, using Irhal-owned source pages first and exact Wikimedia Commons/Wikidata matches second, compressing uploads to compact WebP files in R2 and recording source URL, license, usage status, attribution, and usage notes in Payload Media. Current Karachi coverage is 82 of 411 guide items, with generic Irhal placeholder images denied/unlinked and remaining records left empty until licensed/owned item-specific photos are available.
- Corrected the Quaid-e-Azam House Museum and Wazir Mansion guide-card imagery by removing their shared local Saddar fallback image and linking distinct R2-backed Commons media records with attribution/license metadata: Quaid-e-Azam House Museum uses a public-domain/CC0 Commons image and Wazir Mansion uses a CC BY-SA 4.0 Commons image marked draft for editorial attribution review.
- Removed misleading neighbourhood carousel photo fallbacks from the Karachi homepage. The area cards now follow the same quiet white-card layout as the rest of the page with city label, title, uppercase area metadata, description, and action row; there is no colored top border, generic map icon, or image unless a future verified neighbourhood media model is added.
- Removed presentation-only neighbourhood stubs from the homepage carousel so every neighbourhood card now links to a real internal CMS-backed neighbourhood page. The action label is consistently `Explore area`, and no neighbourhood card links to Google Maps.
- Removed the incorrect Bait-ul-Mukarram Masjid mapped listing from the DHA neighbourhood data. The masjid remains available as the real guide item at `/city/karachi/masjid/bait-ul-mukarram-masjid`, while `/city/karachi/place/bait-ul-mukarram-masjid` correctly remains a 404 because it is not a place page. Listing URL generation is now centralized so mapped listings use the right route for their type.
- Promoted Karachi neighborhood mapping from render-time area string matching to normalized CMS relationships: a new guide-item `neighborhood` relationship and migration were added, the 72 operating-guide areas now seed as real neighborhoods with coordinates/live map queries, and neighborhood pages prefer the relationship before falling back to legacy text matching. Verified `/city/karachi/neighborhood/dha-phase-5` no longer shows Bait-ul-Mukarram, while `/city/karachi/neighborhood/gulshan-e-iqbal` does.
- Added Arabic translations for every normalized Karachi neighborhood and reseeded Payload. Verified `/ar/city/karachi` renders Arabic neighborhood card names/descriptions for `سعود آباد`, `ملير كانتونمنت`, and `بحرية تاون كراتشي`, with the corresponding English fallback text absent from the carousel HTML.
- Added a shared bordered, rounded Discover card action and applied it across the main repeated public card families. Verified `/city/karachi`, `/ar/city/karachi`, and `/city/karachi/section/places-to-visit` render the new action, and browser QA confirms the first visible `Discover` action is present.
- Added shared visible breadcrumbs across city, section, article, item, listing, neighborhood, Islamic travel, itinerary, and news pages in English and Arabic public routes, and removed the duplicate internal section heading on section directories.
- Removed internal implementation/instructional copy from public-facing homepage, section, article, item detail, listing detail, itinerary, Islamic travel, news, metadata, and assistant-response surfaces.
- Updated the global app text color tokens so body text and `text-ink` UI surfaces use `#1E2D4B` consistently across the public app.
- Completed the Google Search Central SEO remediation pass: added generated `robots.txt` and localized `sitemap.xml`, fixed Arabic route canonical/hreflang metadata helpers, aligned JSON-LD URLs with English/Arabic canonical URLs, added `noindex,follow` to search result pages, made root `<html lang>`/`dir` request-aware, and converted remaining English public links away from unprefixed `/city/...` redirect URLs.
- Fixed section pagination SEO so paginated city-section URLs such as `/section/food-and-restaurants/p/2` have their own canonical URL and are included in the localized sitemap, matching Google pagination guidance for distinct paginated content while avoiding dynamic `searchParams` on SSG routes.
- Standardized Arabic as the default public web experience: `/` is the canonical Arabic homepage, `/en` is the English homepage, `/ar` permanently redirects to `/`, and unprefixed inner public URLs permanently redirect to their `/ar/...` equivalents.
- Fixed the public header language toggle so Arabic pages link to canonical `/en/...` English routes and English pages link back to canonical `/ar/...` Arabic routes; also corrected the English header News link to `/en/news`, added document-level locale synchronization for no-refresh client transitions, and added proxy normalization for malformed nested locale URLs such as `/ar/en/city/karachi`.
- Restored meaningful project lint verification by excluding generated `.vercel/**` deployment output from ESLint's global file set.
- Added an R2-aware Karachi hero fallback so public city/homepage hero imagery still prioritizes the approved Mohatta Palace media when Payload CMS is unreachable, and guarded city preload failures to avoid uncaught preload noise during degraded CMS connectivity.
- Made Payload `guide-items` the primary source for public guide item pages and directories, including English body/summary/address, dedicated Arabic title/summary/overview/area/category/address fields, primary image/gallery media, and editor-focused Payload tabs for content, media, place details, SEO, and workflow/source metadata.
- Backfilled Karachi guide-item long English bodies/addresses and Arabic title/summary/overview/area/category/address into Payload only, preserving existing editor-managed fields. The Arabic backfill scanned 411 Payload guide items, completed the final 82 missing rows through direct SQL, found 0 missing Arabic source rows, and the frontend now refuses English guide-item text fallback on Arabic routes by showing Arabic CMS-missing copy until Payload has real Arabic content. Verified every Karachi item with an English body has an Arabic overview, every item with an English address has an Arabic address, and `/ar/city/karachi/place/mohatta-palace-museum` renders the Arabic Mohatta title, full Arabic overview, and Arabic address without the old English overview.
- Closed the final Karachi guide-item Arabic metadata gaps in Payload by adding Arabic area/category/address values for `place/aladin-park-legacy-site` and `place/mohatta-palace-museum`. Follow-up audit confirms all 411 published Karachi guide items now have Arabic title, summary, overview, area, category, and address wherever the corresponding English field exists.
- Backfilled Karachi guide-section article Arabic translations into Payload `guide_sections.translations` and switched article localization to read those Payload translations instead of the local Arabic article dictionary. The backfill updated all 15 Karachi guide sections with 0 missing Arabic article source sections, and Arabic article routes now show Arabic CMS-missing copy instead of English if a future city is missing Payload translations. Verified `/ar/city/karachi/section/visitor-information/visa-information` renders Arabic title/body without the old English article body.
- Backfilled the three Karachi article-level Arabic gaps that remained after strict Payload-only localization exposed them publicly: Visitor Information `annual-temperature-and-climate-guide`, Visitor Information `fast-facts`, and Health and Safety `useful-emergency-numbers`. These records now have Arabic titles, summaries, and table blocks stored in Payload, and the Arabic article renderer shows localized table headings/content instead of the CMS-missing placeholder.
- Codified the Payload-only translation rule in `AGENTS.md` and `docs/cms-import-policy.md`: any translated copy, long description, overview, SEO field, media metadata, image relationship, itinerary, listing, guide item, guide-section, or article content drafted in JSON/scripts/local fixtures is transitional and must be written back to Payload in the same work unless Payload is unreachable. Section title/summary Arabic copy was moved from the component dictionary into Payload `guide_sections.translations`, and runtime section grid/sidebar/detail/article pages now resolve section titles/summaries from Payload. Verified all 15 Karachi guide sections have Arabic title, summary, and article translations in Payload, and `/ar/city/karachi/section/places-to-visit` renders the Arabic Payload section title/summary without the old English title.
- Removed the guide-item frontend media size/filesize rejection so approved Payload media renders even when the current editor-provided asset is small. This fixed Karachi Expo Centre, whose Payload image is approved but only 300×225 and was previously hidden by the public loader. Verified `/en/city/karachi/place/karachi-expo-centre` now includes `karachi-expo-centre.webp`, renders the media gallery, and no longer falls back to the generic place SVG.
- Corrected the regression caused by allowing tiny approved guide-item media to render publicly: Mohatta Palace Museum was relinked in Payload from the 300×225 `place-mohatta-palace-museum.webp` media record to the existing approved 1400×1050 `place-mohatta-palace.webp` asset, and the Payload city loader now rejects guide-item media whose usable hero/card/original image is below 640×360. A media audit found 48 current guide-item records linked to low-resolution media, so those records now fall back publicly instead of stretching blurred thumbnails; they remain a CMS media QA backlog before scaling city imports.
- Vercel data-cache scale fix: the public city loader no longer stores the fully composed `CityGuide` as one `unstable_cache` item. `getCityBySlug` now request-dedupes the recomposed city object while persistent Next/Vercel data cache entries are split into city shell, neighborhoods, listings, itineraries, guide sections, and guide items by kind, each tagged with city-specific cache tags and cache-size telemetry below Vercel's 2 MB item limit. The authenticated revalidation endpoint and Payload hooks now understand precise city fragment tags such as `irhal-city:karachi`, `irhal-city-guide-items:karachi:place`, and `irhal-city-shell:karachi`. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, local Mohatta detail HTML, `/api/search?q=mohatta&locale=en`, and `/sitemap.xml`; the build no longer emits the previous over-2 MB `loadCityBySlug` cache warning.
- Mohatta Palace Museum Payload copy was corrected after production smoke QA: the weak English summary was replaced, Gulf editorial Arabic title/summary/overview were added directly to the canonical `payload.guide_items` record, Aladin Park Legacy Site received its missing Arabic title, and the precise city/guide-item cache tags plus affected paths were revalidated locally and in production. Follow-up audit now shows 0 missing Arabic titles, summaries, or overviews across all 110 Karachi `place` records and 13 `family` records, and production Arabic search returns Mohatta as the first item result.
- Karachi low-resolution guide-item media QA is closed at the public data gate. The audit found 48 low-resolution guide-item media links plus 27 links to media already marked `needs-replacement` (67 total problem links, with overlap by media usage status). Seven guide-item relationships were replaced with existing approved/high-resolution Payload media (Aladin Park, Empress Market, Dolmen Mall Clifton, and Bait-ul-Mukarram Masjid), 17 exact-match Wikimedia/R2 media records with source/license attribution were promoted from `draft` to `approved`, 45 old low-resolution media records were marked `needs-replacement`, and all remaining bad guide-item image relationships were cleared. Follow-up audit shows 0 Karachi guide items linked to low-resolution, draft, archived, or `needs-replacement` media; 33 guide items now have approved usable media and 378 intentionally fall back to placeholders until editorial photo sourcing expands coverage. Revalidated all city/guide-item/search/sitemap cache tags locally and in production, and smoke-tested Dolmen, Empress Market, Bait-ul-Mukarram, and Bagh Ibn-e-Qasim public pages.
- New-city readiness check: the baseline import guard, Payload-only content rules, Vercel cache-fragment split, and low-resolution media gate are now in place. Future city imports still need a media coverage target, but they should not publish guide items with low-resolution, draft, archived, or `needs-replacement` media relationships.
- Relinked Mohatta Palace Museum from the low-resolution 300px media record to the existing 1400px Payload/R2 asset and shortened the public city-data cache to 60 seconds so Payload editorial media/content changes surface quickly on production pages.
- Replaced the low-resolution Merewether Clock Tower hero image with higher-resolution R2-backed Wikimedia Commons media, added a second gallery image with attribution/license metadata, and added English plus Gulf-editorial Arabic history/visit guidance to Payload and repo fallbacks.
- Added Gazebo Restaurant as a Payload-backed Karachi restaurant in DHA Phase 5 / Saba Commercial, including verified map coordinates, source metadata, English and Gulf-editorial Arabic copy, and five user-provided R2 media assets for the public gallery.
- Fixed the guide item media gallery desktop thumbnail grid so four-image side panels render as a balanced 2x2 grid without the stretched row gap seen on the Gazebo Restaurant page.
- Configured Vercel Production and Development environment variables with the Supabase shared pooler `DATABASE_URL`, verified production `/admin` returns 200, and updated local `.env.local` to use the same IPv4-compatible pooler for stable local development.
- Replaced the low-quality Aladin Park Legacy Site image with higher-resolution R2-backed Wikimedia Commons media, retaining CC BY-SA 3.0 attribution/source metadata in Payload and adding a slug-targeted media update script path for future single-item replacements.
- Continued the London Arabic translation rollout in Payload for organized tours, festivals and annual events, shopping, hotels, places to visit, Muslim visitor information, and food and restaurants. The Muslim Visitor Information pass updated 167 masjid/prayer-space guide items, added Gulf-editorial Arabic section/article copy including the Tower Hamlets Council of Mosques directory layer, revalidated London Payload cache tags, and smoke-tested representative Arabic section, article, and masjid routes with no CMS-missing placeholder copy. The Food and Restaurants pass updated all 228 restaurant/food-district records with Arabic title, summary, overview, area, category, and address fields, added Arabic article/table translations for halal verification, neighborhood clusters, curated restaurant verification, and live map layers, passed `npm run typecheck`, and smoke-tested Arabic section, live-map article, and restaurant detail routes.
- Completed the remaining London Arabic translation audit in Payload. City Information and Visitor Information guide-item metadata gaps were closed, the internal Data Resources and Update Workflow article set was translated for CMS completeness, all London guide sections now have required Arabic source-article translations, and the final audit reports 0 guide-item Arabic field gaps and 0 article translation gaps. Final verification passed `npm run typecheck` and smoke-tested 20 Arabic London public routes across city information, visitor information, food, Muslim visitor information, places, hotels, shopping, tours, festivals, and representative detail pages.
- Reopened London Arabic item translation QA after public review showed that many Payload guide-item Arabic fields were present but template-generated rather than editorially equivalent to the English body. Fixed `/ar/city/london/place/tower-bridge` directly in Payload with a full Gulf-editorial Arabic overview, localized area/category/address, revalidated the London place cache tags, and verified the public page renders the long Arabic Tower Bridge overview. Full audit now flags 610 London guide-item records for remediation because their Arabic overview is too short versus the English body, contains template copy, or retains excessive English-facing naming.
- Added `npm run audit:london-arabic-quality` as a strict London Arabic QA gate. The audit fails when guide-item Arabic content is missing, template-like, too short against a long English source body, or contains excessive English leakage. Began remediation by repairing 20 `places-to-visit` records with source-faithful Arabic copy including London Eye, Sherlock Holmes Museum, and Madame Tussauds, verified their Arabic public pages, and replaced bad template language in 555 short-source directory records while deliberately leaving long-body records in the audit. Current strict audit backlog is down from 610 to 340 records.
- Assigned 15 new London user-supplied/partner media assets into Payload with preserved embedded descriptions, attributions, dates, and usage notes. Existing guide items updated: London Eye, Palace of Westminster and Elizabeth Tower, Borough Market place/shopping, London Buses, Tower Bridge, and St Paul's Cathedral. Added new Payload hotel guide items for The Academy Hotel and Threadneedles Hotel with English editorial copy, Gulf-editorial Arabic title/summary/overview/address, official source rows, provider coordinates, map URLs, neighborhood mapping, and galleries. Revalidated London cache tags and verified English/Arabic hotel routes plus representative item routes include the new R2 image filenames.
- Imported all 47 images from `public/guide_photos/**` into Payload Media with embedded metadata, attribution, rights notes, and local source-path traceability. Attached 41 usable photos across London guide-item primaries/galleries, preserving existing primary media while adding multi-use context images for Portobello/Notting Hill, transport, King's Cross, Thames tours, Greenwich, St Paul's, Westminster, Tower Bridge, Spitalfields, Tottenham, and related routes. Added 8 review-state London guide items with English editorial copy, Gulf-editorial Arabic overview fields, coordinates, map URLs, borough relationships, SEO, and source rows: Electric Cinema Portobello, The Churchill Arms Kensington, Horse Guards Parade, Neal's Yard, St Pancras London Autograph Collection, Ranger's House and The Wernher Collection, Big Bus London sightseeing tour, and Brigit's Bakery afternoon tea bus tour. Six narrow/non-priority images were retained as approved Media records but intentionally not attached to public guide items, with reasons captured in `tmp/london-guide-photo-import-report.json`.
- Fixed London transportation article imagery so the `Getting around London` homepage rail, transportation section cards, and transportation article hero pages can reuse matching Payload guide-item media when article records do not carry their own image. Underground/Elizabeth line, National Rail/Overground, Heathrow/Gatwick transfer articles now reuse King's Cross/St Pancras station media, bus articles reuse London bus/Big Bus media, and taxis/driving articles reuse the Seven Dials taxi street image before falling back to generic transport artwork. Verified with `npm run typecheck`, targeted `npx eslint src/lib/guide-items.ts`, and `npm run build`.
- Added a targeted London restaurant media import that uses only approved Irhal-owned or reusable Creative Commons sources, with source URL, license, attribution, photographer, usage notes, and item source rows recorded in Payload. Khan's of Kensington now reuses the Irhal-owned Khan's Restaurant archive image, while Veeraswamy, Le Gavroche, The Ivy, Comptoir Libanais South Kensington, Saravanaa Bhavan, Gourmet Burger Kitchen, and Restaurant Gordon Ramsay use exact Wikimedia Commons images with CC BY/CC BY-SA attribution. The superseded narrow Ivy image was archived, and the public media gate now follows the documented 640x360 cutoff so approved card-quality assets render while tiny 300px media remains blocked. Report written to `tmp/london-restaurant-media-report.json`; verified with targeted ESLint, `npm run typecheck`, DB media-gate audit, and `npm run build`.
- Fixed London hotel rail ranking so the `media` sort no longer treats all media-bearing records equally after the first media check. Public cards now prefer approved media, then recent updates/creation dates, before falling back to description length and title. The public Payload media loader now requires `usageStatus=approved`, preventing draft media from leaking through admin-depth city loading. Generic Marriott campaign creative records #354, #355, and #356 were marked `needs-replacement` because they are not property-specific hotel photography, so the homepage hotel rail should now surface recently added real hotel media such as Threadneedles Hotel and The Academy Hotel ahead of stale/generic promotional graphics. Revalidated local London city/hotel/search cache tags and affected English/Arabic paths. Verified with a DB hotel-order audit, targeted ESLint, `npm run typecheck`, and `npm run build`.
- Published the 9 remaining London guide-item drafts after editorial approval: Electric Cinema Portobello, The Churchill Arms Kensington, Horse Guards Parade, Neal's Yard, Ranger's House and The Wernher Collection, St Pancras London Autograph Collection, Brick Lane Jamme Masjid duplicate/import row, Big Bus London sightseeing tour, and Brigit's Bakery afternoon tea bus tour. Their `_status` and `workflowStatus` are now `published`; a follow-up audit shows 0 London guide-item drafts remaining. Revalidated London city, search, sitemap, and affected place/hotel/masjid/tour English and Arabic paths.
- Redesigned the city homepage hero into a uniform editorial layout: fixed left image carousel and right city introduction panel now share the same desktop height, city-scoped search sits below the hero, prayer times move into a separate strip below search, and the guide category rail follows those utilities. The shared search box now accepts an optional `citySlug`; `/api/search` and `searchSite` support city-scoped results while the global search page remains usable. Verified with targeted ESLint, `npm run typecheck`, a London-scoped `/api/search?q=hotel&city=london` sanity check, and `npm run build`.
- Reduced the city homepage hero height after visual review: desktop hero panels now use a shorter 360-380px presentation instead of the previous 520px block, the city introduction is clamped as a homepage teaser, and a localized `More about the city today` action links to the full city-information article. Verified with targeted ESLint, `npm run typecheck`, and `npm run build`.
- Reordered the city homepage utility stack so the city-scoped search box leads directly into the Explore city icon rail, while prayer timings move below the icons as a supporting utility instead of interrupting the discovery flow. Verified with targeted ESLint, `npm run typecheck`, and `npm run build`.
- Refined the same city utility stack with a thin bordered search field and a compact prayer-times utility bar. The prayer module now uses inline time chips, a next-prayer badge, and a small timetable link so it stays available beneath the Explore rail without adding a tall card. Verified with targeted ESLint, `npm run typecheck`, and `npm run build`.
- Removed the small `City guide` / `دليل المدينة` eyebrow above the city Explore rail and darkened the city search box border from a faint `ink/15` to `ink/35` with a stronger focus state. Verified with targeted ESLint, `npm run typecheck`, and `npm run build`.
- Moved prayer timing out of the separate utility strip and into the dark city introduction card: the next prayer now appears directly below the weather badge as a compact linked pill with a moon icon and arrow to the full prayer timetable. The search-to-Explore flow is now uninterrupted. Verified with targeted ESLint, `npm run typecheck`, and `npm run build`.
- Fixed localized guide-section card ordering so English and Arabic pages sort the same canonical Payload records before applying translations, then preserve that order in the rendered grid/rails. Arabic guide cards now localize hotel tier badges such as budget/moderate/expensive/luxury instead of showing English category tags.
- Extended the canonical-sort-before-localization rule to city homepage guide rails, neighborhood guide grids, article related rails, and item-detail related rails. Top places, hotels, restaurants, shopping, masjids, festivals, tours, family spots, and related guide cards now use the same Payload record order in English and Arabic, with RTL pages only changing reading direction and translated copy.
- Fixed London neighborhood Arabic presentation: Payload now stores Arabic names and descriptions for all London borough neighborhood records, neighborhood carousel cards prefer approved linked guide-item media with a city-photo fallback when no borough item media exists, Arabic neighborhood detail pages localize district/cluster/best-for/live-map labels and Eastern Arabic counts, the schematic map badge is localized, and neighborhood JSON-LD uses localized names/descriptions. Verified with targeted ESLint, `npm run typecheck`, and in-app browser checks for `/ar/city/london` and `/ar/city/london/neighborhood/westminster`.
- Verified the app with `npm run seed:karachi-guide`, `npm run typecheck`, `npm run lint`, `npm run build`, and a `200 OK` response from `/city/karachi`. Latest stability verification: `npm run typecheck`, `npm run lint`, `npm run build`, and in-app browser checks confirmed `/en/city/karachi/place/merewether-clock-tower` has Overview content, `/ar/city/karachi/place/merewether-clock-tower` has Arabic title/overview, `/en/city/karachi/itineraries/karachi-family-weekend` shows `11 stops` with day logistics instead of `0 stops`, and `/en/city/karachi/section/places-to-visit` still lists expected Karachi guide items.

## Remaining / Blocked Work

- Provide remaining real API/environment keys:
  - Cloudflare R2 and Cloudflare Images credentials.
  - Vercel token/project configuration.
  - Google Maps or Mapbox API key.
  - AI provider key.
- Replace placeholder/generated category imagery with real CMS media assets stored in Cloudflare.
- Enrich all location-based guide items with verified latitude, longitude, map provider place IDs, map URLs, neighborhood mapping, source confidence, and verification dates.
- Add richer JSON-LD, dynamic OG images, and canonical production-domain validation.
- Build real search UI and validate Payload search against seeded CMS content.
- Implement AI provider-backed agents with strict JSON schemas, validation, editorial review, and persistence.
- Add multi-city import automation and validation reports so the Karachi format can scale to all world cities.
- Deploy to Vercel with production environment variables.
- Connect the Vercel project to the GitHub repository so pushes can create automatic production/preview deployments, then add Preview environment variables for the chosen `dev` branch.

## Completion Rule

No feature is considered complete unless it is listed here, tested, SEO validated, geo-tagged if location-based, and connected to CMS. Features that have public routes but still require CMS seeding, live API credentials, media storage, or geo enrichment remain `In Progress` or `Blocked`.
